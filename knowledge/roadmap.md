# Breakdown — Product Roadmap

A prioritized list of what to build, grounded in what's already shipped. Horizons,
not dates: each gates on its exit criteria. This is the product/engineering roadmap
only — no go-to-market.

A blocking correctness gate
([**Horizon 0**](#horizon-0--correctness-numbers-the-engine-cant-defend)), three
horizons, plus one standing workstream:
[**Statistical rigor (S)**](#statistical-rigor-s--a-standing-workstream), which
runs alongside them and holds the improvements identified in the
[statistics white paper](statistics_whitepaper.md).

Legend: ✅ shipped · ◑ partially shipped · ○ not started.

---

## Product principles

1. **Build against real need.** Prefer the item a concrete use case is blocked on
   over the speculatively useful one. The roadmap is a priority order, not a promise.
2. **The engine is the core, and it is developed in the open.** The whole
   engine is source-available under [FSL-1.1-ALv2](../LICENSE). Read it, run
   it, self-host it, fork it, build on it, commercially or otherwise. The one
   thing the license excludes is selling breakdown as a competing product, and
   every release automatically becomes Apache-2.0 open source two years after
   it ships. That conversion is an irrevocable grant written into the license
   itself, not a promise. The license points at competitors, not at users.
   Operational surfaces (scheduling, alerting, hosting, multi-user) are a
   separate layer built on top, not woven into the engine.
3. **Never ship a number the engine can't defend.** Credible intervals, a
   first-class `unexplained` term, and suspect-fit flags are the brand. Every new
   surface inherits this honesty posture — no bare point estimates.
4. **Optimize time-to-first-trusted-RCA.** Breakdown should be easy to set up and get value from. The product's north-star metric: elapsed
   time from "here are my credentials" to "breakdown correctly explained an incident
   I already understood." 
5. **Own the last mile; depend on standards, not vendors.** breakdown reads other
   people's semantic definitions, and that is a strength — but a *runtime*
   dependency on a vendor's Python package inherits their release cadence, their
   support window and their Python ceiling. Prefer reading a versioned artifact
   over importing the library that writes it, and keep that library as a
   development-time oracle so drift fails a test rather than a user. The line is
   at *parsing a resolved artifact* (fine) versus *reimplementing a parser* —
   refs, inheritance, Jinja (not fine, and why Sidemantic was rejected). The
   `dbt-bridge` extra depends on nothing from dbt Labs
   ([2.15](#horizon-2--make-it-repeatable-a-stranger-can-onboard)); the engine's
   own definition language stays ours.

---

## Current status (what's built)

**Engine.** Per-node Bayesian Structural Time Series with a non-centered, tightly-
prioritized local-level trend; contemporaneous and lagged regressors; business-unit
priors with automatic rescaling. RCA over the ancestor DAG combining exact **per-day
Shapley** on formula nodes and **posterior attribution** on probabilistic nodes,
with explicit **trend/seasonal component** decomposition and **block-bootstrap**
uncertainty on window means. Fits run on data strictly **before** the analysis window
(uncontaminated by the anomaly). Convergence diagnostics (`fit_quality`) on every fit.
Steady-state **what-if simulation** (interventions, assumption links, Shapley source
attribution). **Cold start mode** (`provider: none`, a *demo* mode — see the note
below): the full what-if machine on a tree with zero data — asserted `baseline`
operating points, coefficients sampled directly from the YAML priors, declared
`plausible` honesty bounds; served end to end (API mode label, guarded routes,
MCP caveats, belief-first UI surface, bundled `cold_start_tree.yml` example). The
same priors feed `fit_metric` when data arrives, so graduation is a provider
swap. *(statistical plan T1–T8; what-if design spec; cold_start_design.md
P1–P5.)*

> **Cold start is a demo mode, not a supported persona (2026-08-05).** It ships
> and stays correct — [C7](#horizon-0--correctness-numbers-the-engine-cant-defend)
> fixes its unbounded baseline draws and
> [S7](#statistical-rigor-s--a-standing-workstream) stays scheduled — but it
> stops earning new roadmap surface. The persona it was built for (a pre-revenue
> founder) has **no incident to explain**, which is the product's actual moment
> of value, and what it produces is sensitivity analysis on stated assumptions —
> a category with incumbents. The follow-on hybrid mode was removed; see
> [Deliberately not on the roadmap](#deliberately-not-on-the-roadmap).

**Providers.** `mock`, `dbt` (a dbt project's own semantic manifest — no dbt
Cloud, no `mf`, and **no runtime dependency on dbt Labs**), `local` (MetricFlow
CLI, superseded per tree by 2.13), `cloud` (dbt Cloud Semantic Layer),
`warehouse` (direct SQL). Provider config supports `${ENV}` secret references.
Per-node `bind:` blocks (2.9) let a tree mix sources, or use none at all.

**UI.** Cytoscape DAG served at `/ui`; per-metric time series + posterior inspection;
full point-and-click RCA workflow (window presets, client-side validation, target
strip, certainty channels, component rows, unexplained badges); what-if tab; node
**stat cards** (big number + delta + sparkline, configurable, format-aware);
deep-linkable RCA / metric / what-if views. *(UI plan U1–U4 + node cards.)*

**MCP server.** Four tools (`get_tree`, `explain_metric`, `run_rca`, `run_whatif`)
over streamable HTTP at `/mcp`, sharing the API process and trace cache; analysis
responses carry `how_to_read` caveats and deep links back into the UI. *(2.5.)*

**Input validation.** Window ordering and overlap, per-node window coverage
(including lagged parents' shifted windows), a gap-free date spine per grain, and
seasonality identifiability (Nyquist harmonic filter + fit-length warnings). *(T9.)*

**Not yet built** (the roadmap below) — and one thing newly finished: **the
Horizon 0 correctness gate closed on 2026-08-17, every row ✅.** C4, C5 and C6
closed on 2026-08-13 (and C19–C22 the same day, from a reviewer reading the
output); the [milestone-readiness audit](milestone_readiness_2026_08_17.md)
added C23–C25 and closed them the same day, along with C12 and 2.20; C9, C13
and finally C7 (the author's option-1 call on the ratio-centre decision)
closed later that day. The punch list with an end has ended; per the
sequencing decision below, the [S track](#statistical-rigor-s--a-standing-workstream)
started after the 0.1.0 release with S1, which closed 2026-08-18 (benchmarked;
full-rank not adopted; S2 promoted to next). Still outstanding: remaining
statistical rigor (T11), UI trust finish (U5–U6), an outsider docs pass before
publication (2.6), and the market-driven items (scheduled monitoring, the rest
of dimensional slicing).

---

## Horizon 0 — Correctness: numbers the engine can't defend

Gate: **blocks everything below.** Principle 3 is not satisfied while these ship.

Every item here is a confirmed defect that produces a *plausible wrong number*
rather than an error — the same failure class Horizon 1.1 was built to close,
found in the places 1.1 didn't look: the provider boundary, the published
statistic, and the agent-facing payload. Most came out of a hostile external
review of the engine, docs, and tests conducted **2026-08-05** against 0.1.0;
**C15 and C16 came out of a second review on 2026-08-12** against `c18d150`,
scoped to the first client deployment and PyPI publication; **C19–C22 came out
of a reviewer running the demo trees and reading the output (2026-08-13,
PR #66)**; and **C23–C25 came out of the
[milestone-readiness audit](milestone_readiness_2026_08_17.md) (2026-08-17)**
against `212b53c`, which traced four recently-decided policies from decision to
code to tests. Each was traced to a specific code path, and the `file:line`
references are the starting point for whoever picks the item up. *(Line refs
re-anchored 2026-08-17 — the 2.16 multi-tree refactor had moved `api/main.py`
by ~1,100 lines and every open row's reference with it.)*

This is not a standing workstream. It is a punch list with an end.

| # | Item | Status | Why |
|---|------|--------|-----|
| C1 | **tz-aware dates must fail loudly, not silently zero** — `pd.to_datetime` on a `TIMESTAMP` column preserves tzinfo; `floor_period` calls `.normalize()`, which preserves it too, so the alignment guard at `data_fetch.py:414` passes. The tz-aware index then matches nothing in the tz-naive `period_spine`, `notna().any()` is False so the trailing trim is skipped, and `fillna(0.0)` returns a **full spine of zeros**. Coerce to tz-naive (warning when tz was present) and raise when the spine join drops every row. Shipped as two shared module-level helpers in `data_fetch.py`: `_to_naive_dates` (drops the zone, keeping the labelled wall-clock date — converting through UTC would move a `+09:00` midnight back a day) runs before anything else touches the dates, and rows that *all* miss the spine now raise instead of zero-filling, since a query ignoring its bound window is a bug rather than an all-quiet series | ✅ | **Ship first.** Any SQL returning `TIMESTAMP` rather than `DATE` (Databricks attaches UTC) turns a whole metric into zeros. `_check_contiguous` passes because the spine is complete, nothing logs, RCA reports a −100% gap and confidently attributes it — and then `snapshots.py` writes the zeros to parquet and serves them forever. A transient connector setting becomes committed data |
| C2 | **Snap `cloud` and `local` to the period spine** — `CloudDataFetcher.fetch_metric` (`data_fetch.py:176`) and `LocalDataFetcher.fetch_metric` (`:261`) floor the labels and return; only `warehouse` and `mock` call `period_spine` and drop partial edge periods. Lift the warehouse fetcher's spine/trim/kind-fill block into a shared `BaseDataFetcher` helper and call it from all three. Same change: **warn** on interior gap-fill instead of silently zeroing a flow. Shipped: `_align_to_spine` holds the spine/trim/kind-fill contract and all three providers call it, so partial edge periods are dropped everywhere and interior gap-fills log the periods they invented. Label policy stays per-provider by design — warehouse errors on a misaligned label (the SQL author owns the aggregation), the semantic-layer providers floor with a warning (dbt may use non-Monday weeks) | ✅ | A window ending Tuesday on a `grain: week` metric returns a two-day partial week as a full row at ~2/7 normal volume, and `snap_window` treats it as whole — a manufactured −71% gap. `/meta`'s `data_through` then reports coverage through days that were never fetched. A three-day ETL outage becomes three zero days, indistinguishable from a real collapse. The mock provider snaps correctly, which is exactly why the test suite never sees this |
| C3 | **Publish the exact Shapley value as `estimate`** — `rca.py:553` reports `float(phi_b.mean())`, the bootstrap mean of a *nonlinear* function of jointly-resampled window means, while `unexplained` (`:586`) comes from the separate **exact** call. `shapley_attribution` already returns exact per-parent values in `result["attribution"]`; use them, and keep the bootstrap for the interval only. Flip `test_rca_formula_attribution` from its 5% tolerance to exact and re-pin the golden. Shipped: `estimate` is now `sh["attribution"][p]` and the two-level `decomposition` parts are exact too, so the split sums to the estimate as exact values rather than in expectation over replicates; the bootstrap supplies `ci_95` and `prob_same_direction` only. The golden test was re-pinned (`gap` and `unexplained` unchanged — they always came from the exact call) and now also asserts the reconciliation property, which is what let the bug survive a golden test in the first place. Residual on the pinned tree: 3.78 → 4.5e-13 | ✅ | Joint resampling induces `Cov(x̄,ȳ) ≠ 0` and the means-bridge is bilinear, so `E[φ_boot] ≠ φ_exact` — contributions don't reconcile with `gap`, contrary to [`docs/model.md`](../docs/model.md). The golden test encodes the discrepancy today (`gap − unexplained` off by 3.78). We compute both numbers and publish the wrong one; on a ratio with a noisy denominator the bias is unbounded in principle |
| C4 | **Bootstrap degeneracy guard and short-window attenuation** — two parts. (a) `single_period` (`rca.py:465`) keys on `n_periods == 1`, so a window in which a parent is *constant* (unlaunched feature, zero-inflated series) collapses every replicate to the same value and ships a **zero-width `ci_95` with `ci_status: "ok"`**; key the guard on the resampled spread instead. (b) `_block_bootstrap_indices` (`:188`) caps the block at `n // 2`, which lands on the midpoint of the degeneracy curve it is reasoning about — the resampled variance of the window mean is attenuated, worst on short windows, and non-monotone in `n`. *Data-driven block length stays [S6](#statistical-rigor-s--a-standing-workstream)*. **Confirmed in production (2026-08-11)** by the Northern Nights deployment, which promotes (a) from a hypothetical to a measured failure: a festival's `flip_comms` intensity node is identically zero across the whole reference window, and the shipped attribution reported `ci_95 = [0.00, 24.94]` — a lower bound sitting exactly on zero — with `ci_status: "ok"`, on the parent the analysis then ranked **first** at ~89% of the gap. Seasonal businesses hold a parent at zero for months at a time, so this is their default state rather than an edge case. **Shipped 2026-08-13**, both parts. *(a)* Two tests rather than one, because keying the status on the published width alone flags every additive identity in every tree — a linear formula's co-movement term is exactly zero for every replicate by construction. So: a **cause** test on the resampled inputs (`max − min ≤ 1e-9 × level`, relative to the parent's own level so a metric in millions and a rate behave alike) sets `ci_status: "degenerate_bootstrap_spread"`, and an independent **effect** test withholds any `ci_95` whose width is ≤ 1e-9 of the node's level, taking `prob_same_direction` with it — identical replicates otherwise report 1.0, a confidence read off no information. Both paths, formula and posterior. Settled as one invariant while there: **no published `ci_95` is ever zero-width**, which also removed a structurally-absent `components.seasonal` reporting `{estimate: 0.0, ci_95: [0.0, 0.0]}` — a zero-width interval about a term the model never had. *(b)* The cap is `n // 4`, chosen from measurement, not from taste: the old `n // 2` gave a resampled-to-true variance ratio of **0.46–0.63 for every n ≤ 16** and was non-monotone (0.55 at n=13, 0.50 at n=14); `n // 4` gives 0.74–0.90 across all n with a non-decreasing block rule, and is on or within 0.03 of the optimum on an AR(1) at ρ=0.6. The old cap was not a trade-off — it sat on the wrong side of the peak for dependent data *and* halved the variance for iid | ✅ | Formula-node CIs come **entirely** from this path with no offsetting term, and [`docs/model.md`](../docs/model.md) sells the bootstrap as the honesty mechanism for exactly the short "what happened this weekend?" windows where the attenuation is worst. A too-narrow interval on the engine's most-quoted number violates principle 3 twice over |
| C5 | **`ranked_causes` inverts on near-zero gaps** — `rca.py:751` uses `min(abs(share), 1.0)`, and `share_of_gap` is `None` only when `abs(gap) < 1e-12` (absolute, not relative to node scale). A node with `gap = 1e-6` and two parents at `+0.5` / `−0.5` yields shares of ±5×10⁵, both clamped to **1.0**. Make the guard relative to node scale and penalize `\|share\| ≫ 1` rather than saturating at it. **Amended 2026-08-12 — the near-zero framing above is too narrow, and the row understated its own reach.** Regenerating the README's MCP transcript surfaced a second trigger with nothing pathological about it: on the **bundled demo tree**, over an ordinary two-week window, `ranked_causes[0]` is `order_count` with a score of exactly **1.0** — clamped from a true share of **1.653**. The gap is not near zero (+$596/day on $26.4K); the share exceeds 1 simply because two parents pull in opposite directions, which is the common case the unclamped `share_of_gap` design exists to express. So the defect is not an edge case reachable with a contrived tree: it fires on the first tree a new user opens, on the number the UI shows most prominently, and it reads as certainty (`1.0`) exactly when the split is *least* settled. Fixing this should treat "share exceeds the gap" as the primary case and near-zero gaps as the degenerate tail of it, not the other way round. **Shipped 2026-08-13.** The guard is now relative to node scale (`\|gap\| ≤ 1e-12 × max(\|baseline\|, \|actual\|)`), and the same absolute-epsilon mistake was found and fixed in two more places in the file, `relative_change` among them — it was a habit, not a slip. The weight is `min(\|s_p\|, 1) / max(1, Σ_q \|s_q\|)`: the denominator is the node's **cancellation factor**, how much gross parent movement the decomposition needed in order to net out to the gap. A share far past 100% now means the split is *less* well-identified, which is what it always meant. Three consequences worth recording: a single-parent node collapses to exactly `min(s, 1/s)`, so 165% scores 0.61 and ranks below a clean 80%; a node's parents' weights sum to ≤ 1, so **a hop can no longer inflate influence** (each parent could previously take the child's full score); and the accumulation tie the row below predicted is broken the right way — noisy ≈ 5e-4 against a clean explainer's 0.5. On the bundled demo the leader is unchanged but honest: `order_count` 1.0-by-saturation → **0.4406**. Same change drops ranked rows no hop ever reached (`via` is never `null` now), since a metric with no attributive path is not a triage candidate and every consumer was inventing that filter independently | ✅ | The metric that most conclusively did *not* move hands its full influence score to everything above it, and scores accumulate across children — so a well-connected node with several quiet children can top the ranking on pure offsetting noise. `ranked_causes` is the most prominent number in the UI. "Documented as a heuristic" does not cover "inverted on a specific, common input" |
| C6 | **Reject duplicate metric names** — `parser.py:479` adds nodes then edges in two passes with no uniqueness check, and `MetricTreeConfig` (`:466`) has no validator. The second definition wins `nodes[name]["definition"]` while **both** metrics' edges are added. **Shipped 2026-08-13** — and it was one validator, firing before `_build_dag` so a merged node cannot exist even transiently, naming every duplicate with its position in `metrics:` and each definition's `source` as a fingerprint. **Three more silent name collisions came out of asking what else collides the same way**, which is the part worth recording: *(i)* **duplicate YAML mapping keys** — PyYAML keeps the last, so a repeated `priors:` or `dimensions:` entry was discarded with no error anywhere; fixed at the loader, so every mapping in every tree is covered at once, and it catches the YAML 1.1 trap from the other direction too (`on:` and `true:` are one key). *(ii)* **`parents: [a, b, a]`** — silent, and the sharpest: `defn.parents` ends up longer than `predecessors`, and the UI reads `beta_raw[i]` positionally off `defn.parents`, so every coefficient after the repeat was *displayed against the wrong parent*. The load-bearing invariant failing in the one surface a user reads. *(iii)* **duplicate seasonality names** — not silent but opaque, dying at fit time with PyMC's `Variable name sin_weekly_h1 already exists`, an error that never mentions the tree. A metric listing *itself* as a parent was already refused by the cycle check, and dimension-name shape was already validated. Same change adds the parse-time half of 2.18's Shapley parent cap, so an author learns at tree load or from `breakdown doctor` rather than five minutes into an RCA | ✅ | `list(dag.predecessors(name))` is the axis order of `beta_raw` — the load-bearing invariant AGENTS.md names. With a duplicate, `predecessors` returns the union while `priors`, `lags` and `expected_signs` are validated against the winning `defn.parents`, so every declared prior lands on the wrong axis or silently falls back to `Normal(0,1)`, and `_fetch_all_metrics` overwrites the data too. Nothing raises. One validator fixes it |
| C7 | **Bound cold-start baseline draws** — `engine/simulate.py` computed `sigma = (high − low)/(2·z90)` and called `rng.normal(mu, sigma, n_draws)` unconditionally; declared `plausible` bounds were loaded but consulted only *post hoc* for extrapolation flags, never at sampling time. Truncate at `plausible` min/max, add a **LogNormal** baseline option (`LogNormal` already exists for edge priors), and stop reporting the Monte-Carlo **mean** as the central number on ratio nodes. **Shipped 2026-08-17, as option 1 of the decision the milestone report framed** — with one deliberate deviation from this row's third clause, recorded because the row said the opposite. *(a)* `_baseline_draws` truncates to `plausible` bounds by **rejection resampling, not clipping** — clipping piles a point mass on the bound, turning "customers cannot be negative" into "there is a 1% chance of exactly zero customers", a belief the author never stated; a `baseline` whose mass lies almost entirely outside its own `plausible` range is a contradiction and raises with both declarations named, since only the author knows which is wrong. *(b)* `baseline: {distribution: LogNormal}` reads `[low, high]` as the central 90% interval on the log scale (`low > 0` required — no support at zero), spelled like the edge priors' distributions. *(c)* the central number **stays the Monte-Carlo mean, made to exist** rather than being replaced: a formula dividing by a belief whose draws cross zero is refused at simulate time, naming the divisor and the remedies (a `plausible` floor, a LogNormal, a tighter belief) — `divisor_expressions` walks the formula AST, counting a constant negative exponent as a division in costume. A median centre was considered and rejected: the mean's linearity is what keeps per-source Shapley contributions summing exactly to `delta.estimate`, and trading that away on the least-evidential surface would spend the additivity brand to avoid a refusal. The bundled `cold_start_tree.yml` needed nothing — every belief already declares `plausible: {min: 0}` and its only formula multiplies | ✅ | On the shipped `cold_start_tree.yml`, `paying_customers` draws ~1% **negative customer counts** and `mrr` inherits them. On ratio formula nodes `base_mu[n] = base_draws[n].mean()` takes the mean of a Cauchy-like ratio, which does not exist: a founder saying "somewhere between 2 and 40 signups a month" — an ordinary order-of-magnitude belief — gets a **$2.1M CAC** with a negative lower bound. Cold start is a demo mode now, and a demo that prints a negative customer count is worse than no demo |
| C8 | **Trace cache, concurrency, and the NaN 500** — `app.state.traces` has no eviction, no TTL and no size bound, and its key `(name, fit_end)` (`api/main.py:504`) ignores inference method and `draws`, so `POST /analyze/{name}?draws=50` poisons the key `run_rca` later reuses. `/meta` (`:393`) and `_pick_fit` (`:155`) iterate the dict outside `app.state.lock` while `run_rca` mutates it from a worker thread. `slices.py:326` writes `np.nan` into `share_b` on near-zero reference totals, which reaches `np.percentile` (`:268`) and then Starlette's `allow_nan=False` as an unhandled **500** Shipped: all four parts. `/meta` and `_pick_fit` snapshot the dict with `list(...)` before iterating (the regression test pins the switch interval at 1e-6 to make the race near-certain — ~95% of iterations against the pre-fix code, 0% after); `_remember_fit` bounds the cache at `MAX_CACHED_TRACES = 256` with insertion-ordered eviction and refuses to let a *worse* fit displace a better one (NUTS over ADVI, then draw count), which keeps the deliberate confirm-with-NUTS upgrade working while blocking the accidental 50-draw downgrade; and `_excess_fields` drops non-finite replicates before `np.percentile`, withholding the interval when too few survive rather than emitting a NaN the encoder turns into a 500 | ✅ | The reference tree has 107 metrics; one RCA adds ~100 `InferenceData` objects and a second `analysis_start` adds 100 more — the user OOMs their own process. The poisoned key falsifies `shaping.py:133`'s promise that the deep link reproduces the numbers. The unlocked iteration raises `dictionary changed size during iteration` precisely while the UI polls a long RCA. *(The wider auth surface is deliberately **not** here — the default bind is loopback and `BREAKDOWN_API_TOKEN` gates `/mcp`; that belongs to [3.5](#horizon-3--make-it-findable-and-sticky-it-comes-to-you).)* |
| C9 | **The agent payload double-counts the interaction** — `compact_rca` (`mcp/shaping.py:257-262`) drops each contribution's `decomposition` but **keeps** the node-level `interaction`. Contribution `estimate` is already `means + comovement` (`rca.py:549`) and `interaction` is the sum of exactly those comovement parts. Drop or relabel it. Same item: `components` is collapsed to `{k: v["estimate"]}`, so the trend number reaches the narrator with no uncertainty at all, and `WHATIF_HOW_TO_READ` (`shaping.py:110`) says contributions sum to the delta *estimate* when they sum to the *point* delta. **Sharpened 2026-08-17:** `interaction` appears in `shaping.py` exactly once — the line that emits it. It is **never mentioned in `RCA_HOW_TO_READ`**, so the payload whose differentiator is that caveats travel with it ships an undocumented field that invites exactly the double count; the fix is a `how_to_read` sentence as well as a schema change. Budget the README MCP-transcript regeneration — the payload shape is pinned by `tests/test_docs_examples.py`. **Shipped 2026-08-17:** `compact_rca` now **drops** `interaction` rather than relabelling it — the compaction already drops the per-contribution `decomposition` that explains it, and a summary of a dropped detail, shipped without the detail, reads as one more term to add; the full HTTP payload keeps both halves together, so nothing is lost, only the invitation to double-count. Zero how_to_read prose spent (the guides have a hard 3200-char budget in `test_mcp_shaping.py`; field absence needs no caveat), and a test now pins `interaction` out of the compacted node so it cannot silently return. `components` keep `{estimate, ci_95}` per component, withheld intervals staying null; `WHATIF_HOW_TO_READ` now says contributions sum to `delta.estimate`, not the posterior. One expected trap that didn't fire: the README's MCP transcript needs no regeneration — it quotes only HTTP-payload figures (estimates, CIs, `unexplained`, `ranked_causes`), all untouched, and `test_docs_examples.py` passes unchanged. One stale doc found in the sweep: `docs/ai-context/python-backend.md` still carried the ⚠️ claiming `compact_rca` shrinks failed nodes to `{status, grain}`, fixed long since — removed | ✅ | An LLM narrating "parents contributed X and Y, plus an interaction of Z" double-counts the entire co-movement term. The MCP surface is the strategic asset — `how_to_read` caveats travelling with the payload is the idea nobody else has — and a payload that invites arithmetic error undoes it. Cheap to fix, and the failure is invisible in review because the tree used to demo it is multilinear |
| C10 | **Bring `b2b_mrr_tree.yml` up to the schema it is the reference for** — audited: 107 metrics, 47 formula nodes, **6 probabilistic**, 114 edges of which 100 are deterministic; and **zero** `grain`, `kind`, `dimensions`, `expected_signs`. All 31 rate-shaped metrics inherit `grain: day, kind: flow`, violating the guidance the README sets in bold; `total_mrr` and `total_customers` are described as stocks and declared as flows; `total_email_subscribers` is defined as the period's *net add* and then multiplied by send frequency. Add `grain`/`kind` throughout, `dimensions` on sliceable nodes, `expected_signs` on the 14 learned edges, fix the net-add definition and the `product_qualified_leads` collinearity. Consider a parse-time lint for rate-shaped names inheriting `kind: flow`. Shipped: the tree is now 106 metrics / 112 edges, cut **day + month** (acquisition funnel daily, MRR waterfall and enterprise close monthly) with three documented day→month handoffs; `kind` declared on every node — **46** rates, not the audited 31, because the audit's name heuristic missed the 8 `average_*` nodes, the 6 durations and the 2 per-unit intensities, all of which are equally non-aggregatable; 6 stocks; `dimensions` on 26 nodes (55 declarations) over region / plan_tier / segment / channel, with `weight` wired on every rate; `expected_signs` on all 12 learned edges. The PQL collinearity was fixed rather than annotated (rates on rates); `opened_new_business_opportunities` became a formula over a named `sql_to_opportunity_rate`, removing the last level-on-level edge; `total_email_subscribers` became a leaf stock and its two net-add parents were dropped. Weeks are impossible here, not merely unused — every branch converges on `total_mrr` and weeks do not nest in months. Also shipped: the parse-time lint (a **warning**, and only when `kind` was left at its default, so `kind: flow` written explicitly is the escape hatch), `tests/test_reference_tree.py` (nothing loaded this file before, which is how it drifted), and `kind: rate` on the bundled jaffle example's `average_order_value`, which the new lint caught immediately | ✅ | It is cited as the worked reference tree, so it is the answer to "how hard is this to author" and the shape a new author copies. Today it would mis-model 31 rates, mis-aggregate 3 stocks, carry a definitional error and a structural collinearity, and — with zero `dimensions` — cannot run the slicing workflow at all. Largest item here, and the one most likely to surface further engine gaps while doing it |
| C11 | **The mock provider ignores the schema it is generating for** — `MockDataFetcher._tree_data` (`data_fetch.py:648`) draws every root from `rng.uniform(50, 5000)` regardless of `kind`, so a `kind: rate` leaf is generated on the same scale as an impression count. In a funnel of `count × rate` identities the error compounds once per level: on the reference tree `total_mrr` means **1.5×10²⁵**, and every node with `format: percent` renders a five-digit percentage. Same function, second defect: probabilistic nodes use `coefficient`'s `mu` for *all* parents, falling back to `rng.uniform(0.1, 0.5)` — per-parent `priors` are never read and no coefficient is ever negative, so a correctly declared `expected_signs: negative` edge produces a **guaranteed spurious `sign_warnings`**. Generate rates in (0,1), durations/intensities on a small positive scale, and read per-parent priors. Shipped: leaf generation is kind-aware (`_mock_rate_scale` — a share in (0,1), or a per-unit magnitude for a duration/average/`_per_` name, both clipped to their band and given a damped walk, since an undamped one drifts a share negative over a long window); learned edges read the **per-parent** prior with a distribution-aware representative value (`_mock_coef`: `Normal` → `mu`, so a declared negative really is negative; `HalfNormal` → `E[·]`); and a probabilistic **rate** child now takes parent *deviations* around its own level rather than `sum(coef × parent)`, which had put a conversion rate on its regressors' scale — the composite is rescaled by a positive scalar, so every parent keeps its sign and its share of the signal. Flows and stocks are untouched, so all-flow trees stay byte-identical (the pre-grain golden still passes unchanged). On the reference tree `total_mrr` went from **1.5×10²⁵ to ~$3.2M**, ARR to ~$38M, every rate landed inside its band, and the 3 spurious `sign_warnings` went to **0** | ✅ | The mock is the demo, the tutorial, and most of the test suite. C7 already established the standard — "a demo that prints a negative customer count is worse than no demo" — and this prints a $10²⁵ MRR. Worse, the sign defect makes the engine's own honesty diagnostic cry wolf on exactly the trees that use it correctly, training authors to ignore it |
| C13 | **The mock generates every leaf independently, so a difference identity goes negative** — `MockDataFetcher._tree_data` (`data_fetch.py:1088-1180`): `controllable_attrition = cancel_requests − saved_cancel_requests` on the reference tree draws both leaves as independent random walks with nothing holding `saved ≤ requests`. **Measured 2026-08-13 over 2024-01-01 → 2024-12-31, and the row was wrong on two counts.** It is not that the difference *can* go negative: `controllable_attrition` runs −3,043.0 to −2,471.7 and is negative in **100% of periods**, because `saved_cancel_requests` (3,357.4–4,214.8) exceeds `cancel_requests` (924.8–1,220.2) in every one — the mock puts the two leaves on unrelated scales, so the node never once holds a possible value. And `churned_customers`/`churned_mrr` do **not** inherit it: `churned_customers = controllable_attrition + uncontrollable_attrition` stays positive (1,467.8–2,413.6) because the uncontrollable term is larger. Blast radius is one node, not three, and there is no churn that adds MRR. Both corrections came from running the tree and reading the series, which is the [`read-the-numbers`](../.claude/skills/read-the-numbers/SKILL.md) practice doing exactly what it is for. The generator walks the DAG in topological order and could constrain the leaves of a subtraction (generate the subtrahend as a share of the minuend), or fall back to non-negative clipping on `flow` nodes whose formula is a difference. **The 2026-08-17 audit settles the choice**: clipping is ~5 lines but produces a worse demo — with the leaves on unrelated scales (3,357–4,215 vs 925–1,220) the clip yields a series identically 0 in 100% of periods, which then trips C4's `degenerate_bootstrap_spread` guard and renders as a withheld interval on a node that should just be a normal number. Subtrahend-as-share-of-minuend is the fix; budget the golden re-pin, since all-flow trees are byte-identical today. **Shipped 2026-08-17**, as the constraint rather than the clip — clipping is ~5 lines, but with the leaves on unrelated scales it yields a series identically 0 in 100% of periods, which trips C4's `degenerate_bootstrap_spread` guard and renders a withheld interval on a node that should just be a normal number. `MockDataFetcher._difference_constraints` maps subtrahend-leaf → minuend-leaf for every formula that is a plain `Name - Name` over two same-grain flow leaves, and the generator draws the subtrahend as a damped-walk share of the minuend clipped to (0.1, 0.9) — so `saved ≤ requests` holds *per period*, the subtrahend inherits the minuend's level and seasonality, and the share still varies (it is not a constant fraction a fit would see as collinear). Deliberately out of scope, documented in the method: multi-term differences (`a + b - c`), a formula or learned node on either side, rate-kind leaves (already banded), and mixed-grain pairs. Measured post-fix over the same window: `controllable_attrition` **311.2–433.3, negative in 0% of periods** (was −3,043.0 to −2,471.7, 100%); `cancel_requests` byte-identical at 924.8–1,220.2; `saved_cancel_requests` 601.1–793.2 (share of requests 0.644–0.668, σ=0.0068); `churned_customers` 4,565.5–5,695.8, still positive. Difference-free trees are byte-identical by construction — the pre-grain golden (`test_mock_all_day_tree_pinned_values`) did not move — and the property is pinned on both a minimal tree (`tests/test_data_fetch.py`) and the reference tree itself (`tests/test_reference_tree.py`) Distinct from [C11](#horizon-0--correctness-numbers-the-engine-cant-defend), which was about *scale*; this is about *sign*, and it survives C11 | ✅ | Smaller blast radius than C11 — 3 nodes on the reference tree, bounded magnitudes, no compounding — but the same class of defect: the demo shows a number the business it is modelling cannot produce, and a reader cannot tell the fixture's limitation from an engine bug. Cheap to fix once someone decides which of the two strategies is less surprising |
| C12 | **A rate's slicing `weight` must share its grain, but nothing checks until slice time** — `_run_slice` (`api/main.py:1731-1739`) raises when `weight_defn.grain != defn.grain`, which is the first moment anyone finds out. `Parser._validate_dimension_weights` (`parser.py:1600-1611`) already walks the DAG with both definitions in hand and already validates that the weight names a real metric; the grain agreement belongs in the same loop. **Sharpened 2026-08-17, two ways.** *(i)* "500s" below is stale — the endpoint now catches `ValueError` → **422**; the substance (a runtime failure on a tree that parsed, booted and RCA'd clean) holds. *(ii)* It is not just a late check but a **policy contradiction between two files**: `_validate_denominators` (`parser.py:1485-1498`) accepts a denominator at a *finer, nesting* grain, and `dimensions[].weight` defaults *from* `denominator` — so a month-grain rate over a day-grain denominator parses clean, computes its window aggregate correctly through `weights_for`'s resampling, and then 422s on the first slice click against `_run_slice`'s *strict equality*. Moving the check to the parser forces the policy decision first: strict equality (simplest; newly rejects trees that parse today — a breaking schema change under the pre-1.0 contract) or accept-finer-and-resample in `_run_slice` (consistent with `weights_for`, breaks nothing, ~40–60 lines). No shipped tree currently has a mismatch, so either lands safely now. **Shipped 2026-08-17.** Strict equality at parse time, scoped to where slice time is reachable: the check lives in `_validate_dimension_weights`, one method above `_validate_grains`'s cross-node work, and fires only when the rate declares `dimensions` — a finer-nesting denominator stays legal on a rate that only aggregates (the window aggregate resamples; the sliced blend does not), and becomes a parse error the moment slicing becomes reachable. The `_run_slice` raise stays as a backstop for callers who assemble a DAG without the parser | ✅ | A tree parses clean, boots clean, serves `/meta` and the whole tree RCA clean, and then fails the first time a user clicks a rate node's **slice by** row. Authoring the reference tree hit this and had to route around it by hand — the check exists, it is just three seconds too late to be useful |
| C14 | **The deployed demo served 503 to every first visitor** — three compounding causes, found by timing the public instance. (a) `engine.model` imported `pymc`/`arviz`/`pytensor` at module scope and sits on `api.main`'s import path; they are ~80% of the process's import cost, and uvicorn cannot bind until module import finishes, so the container took **43s** to serve its first byte (the VM itself was up in 1.0s) while Fly's proxy gave up at ~46s. (b) `demo/Dockerfile` ran the server through `uv run --no-sync`, whose project discovery costs ~0.5s on a *warm* cache and every container start is cold. (c) the UI made a single `/health` request and rendered nothing if it failed, so the failure mode was a blank page rather than a message. Shipped: the three imports moved into the five functions that use them (`_PRIOR_DISTRIBUTIONS` became a frozenset of names resolved with `getattr`), `lifespan` warms them on a daemon thread *after* the data load so the cost does not simply move onto the first *Run analysis* click, `CMD` calls the console script directly, and `apiWithWake()` retries 502/503/504 and dropped connections with backoff behind a "Waking the server up…" status and a **Try again** banner. `import breakdown.api.main` 2.50s → 0.97s; local launch-to-serving 2.48s. Three tests pin it, because one convenient top-level `import pymc` silently undoes the whole thing | ✅ | The demo URL ships in the v0.1.0 release notes and the PyPI sidebar — the two places a stranger's first click lands — so this was the product's first impression failing closed. **The 503s are not fixed by this alone:** the Fly account is on a trial that force-stops machines after **5 minutes** of runtime regardless of traffic or config, which no `fly.toml` setting overrides and which also discards the pre-warmed trace cache `demo/prewarm.py` exists to fill. Separately, `min_machines_running = 1` has never reached production: the `Deploy demo` workflow has failed **6/6** on a missing `FLY_API_TOKEN` (no repo or `demo`-environment secret exists), so the live machine still runs the Aug 4 config. Both are account/CI actions, not code |
| C15 | **A dbt metric's `filter` is silently dropped — the node serves the unfiltered measure** — `dbt_manifest.py:130`. `Metric`/`MetricInput` don't model MetricFlow's `filter` (nor `fill_nulls_with` / `join_to_timespine`), `_Node` sets `extra: "ignore"`, and `_translate_simple` never checks for one, so a filtered dbt metric translates into a filterless `BindingSpec`, is **not** reported in `BridgeResult.skipped`, and the generated SQL aggregates the entire relation. Broader than metric-level `filter`: MSI models `join_to_timespine` and `fill_nulls_with` on `PydanticMetricInputMeasure` and `filter` on `PydanticMetricInput`, so a **ratio** whose numerator or denominator carries a filter, and any measure declaring `fill_nulls_with: 0`, mistranslate the same way — fix the whole set of semantics-changing input fields, not just the one. **Fix now:** skip any metric whose own filter, or whose input's filter/fill/timespine flags, are truthy, with a reason naming the construct, exactly like the existing `non_additive_dimension` skip — `BindingSpec` has no filter field at all, so refusing is the only correct behaviour available today. Real filter support (`where:` on `BindingSpec`, fed by dbt's `where_sql_template`) is ~80% of real trees and is a follow-on item, not a patch — filed as [2.17](#horizon-2--make-it-repeatable-a-stranger-can-onboard). **Shipped:** `WhereFilter`/`WhereFilterIntersection` normalise all four serialisations MetricFlow has accepted (a shape we didn't recognise would have parsed as *no filter* — the same defect again), `MetricInput` splits into metric-ref and measure-input shapes, and one `_unsupported_semantics` guard runs before dispatch so simple, ratio and derived are covered together. Two traps the fix had to avoid, both pinned by tests: `fill_nulls_with: 0` is **falsy**, so a truthiness check would let through the commonest value (the test is `is not None`); and an empty filter intersection is a **truthy** Pydantic model, so refusal keys on a non-empty predicate rather than on the object. dbt writes these in four places depending on spec version and whether the manifest went through its flattening transform — checking one is how this survived | ✅ | **Inverts the product's core promise.** The pitch is that breakdown computes over the semantic layer the client already governs; serving a different number *under the governed metric's name* is the one failure that cannot be argued down as a modelling caveat. `filter` is the single most common way a real dbt metric narrows a measure, and nothing catches it: the bridge succeeds, `skipped` is empty, and `doctor` passes because the grain assertion still sees one row per grain key. The tree is then fitted and attributed against a number several times larger than the governed metric |
| C16 | **Editing a metric's `sql:`/`bind:` doesn't invalidate its snapshot — and provenance then attests the new SQL for the old numbers** — `snapshots.py:39`. `SnapshotStore._filename` keys on `(query_name, grain, kind, start, end)`. For `warehouse` the query *is* the metric's own `sql:` block; for `dbt` it is the node's `bind:` block. Both fully determine the returned values; neither is in the key. Edit a query to exclude refunds, restart, and the server serves the pre-edit numbers forever. **Fix:** add a `definition_sha` to the `manifest.json` record, compare on read, warn-and-refetch on mismatch. **Shipped:** `definition_sha` fingerprints the provider-side definition itself (window-independent — `query_provenance` was rejected as the source because its dbt output is generated per window, so the same binding would fingerprint differently per process and turn every hit into a miss, and `read_sliced` serves any stored window covering the request so its fingerprint cannot mention one). Computed once per call *before* the inner fetch, so a hit and the record it would be written under can never disagree. Sliced path covered. Pre-fingerprint records are served with one warning per file naming `BREAKDOWN_REFRESH=1`: refusing them would break every committed snapshot dir and every snapshot-only deployment, which have no provider to refetch from — the sin here was the silence, not the serving | ✅ | **The provenance inversion is worse than the staleness.** `SnapshotFetcher` delegates `query_provenance` straight through (`snapshots.py:163-173`), so the UI's *show query* panel displays the refund-excluding SQL beside the refund-including number — [2.11](#horizon-2--make-it-repeatable-a-stranger-can-onboard) exists so a reader can verify a number the engine can't otherwise defend, and here it certifies a wrong one instead. This sits squarely in the path of a first deployment, because a first deployment *is* iterative tree authoring against a live snapshot dir, and nothing in `doctor`, `/health`, the manifest or the logs says anything is stale. The `definition_sha` fix repairs both: a hit can only be served when the statement shown is the statement that produced it |
| C17 | **A zero denominator in a formula is an unhandled 500 — and a NaN the agent payload turns into `null`** — `rca.py`. `shapley_attribution` evaluated a formula node with no finiteness guard. Two failures in sequence, and the first was invisible until it was reproduced: `eval_formula` runs under `{"__builtins__": {}}`, and numpy's divide-by-zero *warning* machinery resolves `__import__` from the calling frame's globals, so the very first zero denominator died with **`KeyError: '__import__'`** — a 500 naming nothing about the tree, the node or the zero. Suppress the warning and the NaN propagates instead: `baseline`, `gap`, every attribution, every `ci_95`, `unexplained`, `interaction` and every `ranked_causes` score go non-finite, reaching Starlette's `allow_nan=False` encoder as a second unhandled 500 — and over MCP `round_floats` turns each one into `null`, handing an agent a decomposition of nothing. The NaN also escaped `_rank_causes`' clamp, because `min(abs(nan), 1.0)` is `nan`. **Shipped:** `eval_formula` silences numpy's FP conditions (the sandbox is untouched — re-adding `__import__` would have been the wrong fix and is warned against in the docstring); a `NonFiniteAttribution` subclass carries a diagnostic naming each offending parent series, its window, and the dates (capped at five, like `_align_to_spine`); the bootstrap drops non-finite replicates before `np.percentile` and withholds the interval below `_MIN_CI_REPLICATES`, reporting `ci_status: "nonfinite_bootstrap_replicates"`; a non-finite share weighs 0.0 in `_rank_causes`; and an ancestor that fails this way degrades to `status: "attribution_failed"` with its own `gap` still reported, rather than taking the tree down | ✅ | The trigger is not exotic: `formula: "revenue / order_count"` is the README's own documented shape, `_align_to_spine` *manufactures* the zero by design for a `kind: flow` denominator, and a seasonal business produces off-season zeros naturally. This is the third instance of a policy chosen carefully in one file and not propagated: `slices.py:530-534` already filtered non-finite values before the encoder, with a comment naming this exact failure, and `rca.py` never got it |
| C18 | **A metric that started mid-window is fabricated as zeros, silently** — `data_fetch.py`. `_align_to_spine` computes `interior` as the NaN periods strictly *after* `first_valid_index`, so periods **before** the source's first row fall outside `interior` by construction and are zero-filled by the `kind: flow` branch with **no log line at all**. Reproduced: a source whose first row is 2024-01-20, fetched from 2024-01-01, returns 22 rows of which **19 are invented `0.0`** and nothing is said. The fit then trains on them — a manufactured level shift and a manufactured trend, on a node RCA will rank as a cause. Interior gaps warn and name their periods; `stock` raises (nothing to forward-fill from) and `rate` raises (a rate cannot be invented); `flow` alone was silent. **Shipped:** a warning naming the metric, the count, the fabricated range, up to five periods and what it does to the fit. **Trimming the leading run was considered and rejected on a concrete downstream argument**: per-grain frames inner-join in `build_grained`, so trimming one late-starting node would delete those periods for *every* metric at that grain and `_validate_coverage` would then refuse reference windows tree-wide — a whole tree losing January because one node launched in March. The honest version narrows only the late node's own window, which needs a per-metric window the frames do not carry; recorded as the follow-up | ✅ | The **fifth** silent-wrong-number defect at the provider boundary, and it was sitting inside the shared contract that [C1](#horizon-0--correctness-numbers-the-engine-cant-defend)/[C2](#horizon-0--correctness-numbers-the-engine-cant-defend) built to end exactly this class. It is also the one most likely to meet a *first* client, whose metrics are precisely the ones that start partway into the loaded window — a product launched in March, a channel switched on in week 3. Filed as medium by the review that found it, which is the more interesting fact: a silent fabrication reads as a nicety until you notice the fit trains on it |
| C19 | **`prob_same_direction` saturated at 1.0 and rendered as certainty** — the statistic is `max((x>0).mean(), (x<0).mean())` over 500 bootstrap replicates, so its representable values are k/500 and there is nothing between 0.998 and 1; it published the saturated 1.0 and rendered `P(dir) 100.0%` on 21 of the reference tree's 105 nodes — most likely exactly where the evidence is thinnest, while one line away `rca.py` withheld a degenerate probability as "a confidence read off no information". **Shipped 2026-08-13 (PR #66):** one shared estimator caps at `1 − 1/_N_BOOT` with `prob_same_direction_censored: true` beside the ceiling (present only when it saturated), both renderers print `>99.8%` — *truncated*, not rounded, because `(0.9995).toFixed(1)` is "100.0" — and an exact propagation from a pinned intervention keeps its honest 1.0. Invariant: every published direction probability lies in `[0.5, 1 − 1/_N_BOOT]` | ✅ | C5's "a saturated clamp reads as certainty," on the probability side. Found by running the demo trees and reading the output (`read-the-numbers`), not by either hostile review — both had the mechanism in hand |
| C20 | **The export was missing the two facts its reader most needs** — the static report omitted the fit window on the one node flagged *suspect fit* (the live header shows it; the export is where the reader cannot hover), and a contribution carrying `lag`/`parent_windows` was measured over a *different window* than the section header names, silently, on both surfaces — lagged edges being the demo's signature feature. **Shipped 2026-08-13 (PR #66):** live gets a `lag` chip with the shifted windows in its tooltip; the export prints both outright, because a tooltip is simply absent from a static report | ✅ | 1.5's export is "the thing an analysis becomes when it leaves the app" — it circulates without its author, so a fact present only on hover is absent from the artifact that gets acted on |
| C21 | **An absent `direction` became a claim** — the parser defaulted `direction` to `up_is_good` and `/dag` serializes with `model_dump()`, so the default reached the browser indistinguishable from a declaration and `app.js`'s own fallback could never fire: `churn_arpu` rose 18.5% and rendered **green — "improved"** — while carrying 27.3% of the damage on a node down 32.1%. **Shipped 2026-08-13 (PR #66):** the field is `Optional`, defaulting to `None`; undeclared survives serialization and renders like `neutral` (arrow and sign, no good/bad colour) with a legend row so an untinted card does not read as missing ink. The lesson is cited by [1.11](#horizon-1--prove-it-a-trustworthy-reproducible-rca)'s `no_denominator` design: presence must survive `model_dump()`, so `model_fields_set` is not a serialization mechanism | ✅ | The honesty posture inverted in the one surface a client looks at, from an honest payload — the fifth-rule class. The UI could not do better with that payload, so the fix belonged at the boundary |
| C22 | **A cleared date field was an unhandled 500** — `pd.Timestamp("")` is `NaT`, which satisfies a `str` annotation, survives every `is None` guard and reaches `snap_window`, where `NaT.normalize()` is an `AttributeError`; `analysis_start=banana` 422'd correctly, which is why the empty string went unnoticed — two routes ran the ISO check inline and their four siblings had not. **Shipped 2026-08-13 (PR #66):** every date parameter is one annotated type (`Annotated[IsoDate, Query(...)]`), `ScenarioRequest` carries the body half, `grains.to_date` refuses `NaT` in the engine. Invariant: no date parameter on any route can produce a 5xx for any of five bad shapes | ✅ | The same policy-at-one-call-site shape as everything else in this table, on the input boundary rather than the output one |
| C23 | **The sliced fetch path never got the shared date contract — a tz-aware sliced frame becomes an all-zero panel** — `_sliced_long` (`data_fetch.py:521-551`), used by both `cloud` and `local` sliced fetches, calls `_floor_labels` but never `_to_naive_dates`; the `dbt` provider's sliced path *does* call it (`dbt_provider.py:479`) — the C1 policy propagated to one of three. Downstream, `slices._fill_by_kind` (`engine/slices.py:124-134`) reindexes the tz-aware frame against the tz-naive spine, which pandas answers with **silent all-NaN**, and the flow branch `fillna(0.0)`s it: every slice zero, `baseline = actual = gap = 0`. Reconciliation against the (correct) unsliced series flags `discrepant`, so it is not fully silent — but the on-screen numbers are fabricated, which is verbatim the C1 symptom in a surface C1 never swept. Same row, same surface: `_fill_by_kind` is a **second, independent implementation** of the kind-aware fill with no log line at all (C18's shape one layer up) and the *opposite* stock policy (a leading gap zero-fills where `_align_to_spine` raises); `_pivot` (`:119`) silently **sums** duplicate `(date, slice)` rows where the unsliced path treats a duplicated date as a hard grain-violation error; `_rate_attribution` (`:734`) silently fills missing weight periods; and `SnapshotFetcher.fetch_metric_sliced` (`snapshots.py:512-521`) serves a sliced hit with no `_realign_snapshot` equivalent. **Shipped 2026-08-17**, all four parts: the `_to_naive_dates` call in `_sliced_long` (same warning as the metric path); the rule-1 invariant extended to enumerate `fetch_metric_sliced` (the mock's exemption asserts its *reason*, and a guard pins the coercion inside `_sliced_long` itself); `_pivot` now **refuses** duplicate `(date, slice)` rows, naming the pairs, instead of silently summing; and `_fill_by_kind`'s divergence from `_align_to_spine` is *chosen in writing* — a slice legitimately starts mid-window — with every fill at the layer counted and logged, the weight frame's included. Not built: a full `_align_sliced_to_spine` sibling (partial-edge drop, trailing trim for sliced frames) — the row's "consider", left until a real sliced frame needs it | ✅ | The rule-1 invariant only inspects `fetch_metric`, which is how this survived: the sixth silent-wrong-number defect at a date-alignment seam, found by asking where else the C1/C2/C18 contract *doesn't* reach. Slicing is the feature carrying the thesis (3.2), and `cloud`/`local` are two of the three providers that have it |
| C24 | **A rate's slice panel can never say "localized" — the concentration verdict is dead for the showcase shape** — the UI verdict gates on `top.baseline_share != null` (`static/app.js:3065-3067`) and the rate-attribution rows (`engine/slices.py:825-836`) never emit `baseline_share`; only the sum path does (`:630`). So for every `kind: rate` dimension the panel unconditionally prints *"Not localized by ⟨dimension⟩"* no matter how concentrated the movement — and every existing rate-panel test asserts the **negative** (e.g. `tests/test_white_cube_demo.py:438`), so the suite passes for the wrong reason; no test anywhere asserts a rate panel *can* localize. Same row: the 0.25 concentration floor exists **only in the browser** (hardcoded at `app.js:3066`, hand-mirrored in a test helper) — the payload and MCP publish no `localized` verdict and no threshold, so an MCP consumer, holding `prob_concentrated` (which gates on the bootstrap, a different question), will confidently name the top slice exactly where the UI declines to. **Shipped 2026-08-17**, exactly that shape: rate rows carry `baseline_share`; the verdict is computed beside the numbers (`_localization`) and published as `localized` + `localization_threshold`; `app.js` reads the published field instead of recomputing; the demo suite's `ui_localized` helper reads the same field so the three consumers cannot drift apart again; `compact_slice` carries it (with the 3.8 trio — `additivity`, `overlap`, `entity_flows` — and `how_to_read` clauses, closing the audit's MCP-payload finding in the same pass); withheld shares still force `localized: false`; and the positive assertion exists (`test_a_concentrated_rate_slice_can_be_localized`) | ✅ | "Churn rate fell, concentrated in EMEA" is the product's showcase sentence, and the panel is structurally incapable of saying it. Fifth-rule class — no JS runner, so it survives until someone opens the UI on a concentrated rate and looks, which is exactly what the 2026-08-17 audit did |
| C25 | **`/simulate` is the surface the honesty policies stopped short of** — two halves, one surface. *(a)* A rate's baseline is computed through `node_window_value` (arithmetic right, and the AST invariant passes) but published with **no `window_aggregate`/`window_aggregate_reason`** (`engine/simulate.py:663-688`, `:755-770`); the what-if UI renders the bare number (`app.js:4127`, `:4154` never call `windowBasisHtml`) and `WHATIF_HOW_TO_READ` has no clause — violating [1.11](#horizon-1--prove-it-a-trustworthy-reproducible-rca)'s own rule, "labelled where the number is read, not only in the log," on one of the two surfaces that read it. *(b)* The scenario result returns to Starlette's `allow_nan=False` encoder with **no non-finite sweep** (`api/main.py:1847-1882`) — the 2026-08-12 review's L4, and a live violation of AGENTS.md rule 3 *after* the rule was written down with a claimed structural test; one zero denominator in an unbounded cold-start ratio draw ([C7](#horizon-0--correctness-numbers-the-engine-cant-defend)) away from an unhandled 500. **Shipped 2026-08-17.** *(a)* `window_aggregate`/`window_aggregate_reason` ride on both simulate node shapes (an unaffected rate still shows a baseline), rendered via the existing `windowBasisHtml()` in the card and the per-node table, carried by `compact_scenario`, with a `WHATIF_HOW_TO_READ` clause; cold start stays deliberately unlabelled — its baselines are declared beliefs, which its own caveats already label. *(b)* a final `_refuse_non_finite` pass raises a ValueError naming the offending nodes (the API's 422). Refusal rather than per-node degradation, on an argument worth keeping: RCA degrades per node because a tree's nodes are independent; a scenario's deltas *propagate*, so one non-finite node contaminates its whole downstream and the honest unit of refusal is the scenario | ✅ | The third instance of the C17 pattern by the project's own count — `slices.py` sanitized, then `rca.py` learned it, and `simulate.py` never did — and the labelling half is 1.11's policy failing its first propagation test. Both halves are hours, not days |
| C26 | **`non_physical` is a one-sided check, and a rate has two impossible sides** — `engine/simulate.py:782` fires the flag only on `simulated < 0 and hist["hist_min"] >= 0`. A `kind: rate` node with a `denominator` is a share of that denominator: it cannot exceed 1, and the tree says so structurally, without consulting history. Measured 2026-08-22 on the White Cube demo, baseline `2026-06-29 → 2026-07-26`: `member_activity_rate` +300% simulates to **1.025** — more members active than exist — and the response carries one `extrapolation` warning ("above the historical max 0.3162") and **no `non_physical`**, while the very same run correctly flags `non_physical` on `customer_churn_rate`, `churned_subscriptions` and `churned_mrr` for going negative. Two impossibilities, one flagged as impossible and one as merely unusual. The asymmetry is invisible today only because the tour's scripted absurd lever pushes churn *down* past zero; before the 2026-08-22 generator fix a routine **+20%** on activity would have landed at 1.11, so the demo was one plausible click from printing it. This is [C25](#horizon-0--correctness-numbers-the-engine-cant-defend)'s finding one door further along — the honesty policy exists in `simulate.py`, is reasoned about in place, and covers one bound of two. Scope: raise `non_physical` for a `kind: rate` node with a declared `denominator` whose simulated value exceeds 1 (never from history — history is what makes the negative test conservative, and a share's ceiling is not an empirical fact); the warning must reach the what-if card, the per-node table and `compact_scenario`, exactly as the negative one does. **An invariant should carry this**: every physical-bound check in `simulate.py` enumerated against the node kinds whose bounds are structural, so the next one-sided check fails `tests/test_project_invariants.py` rather than waiting for someone to move a lever | ○ | The what-if tab's pitch is the sentence the guided tour scripts out loud: *it is telling you the scenario is nonsense rather than returning a confident number.* That sentence is true in one direction and false in the other, and "103% of members were active" is exactly the output a prospect screenshots. Rule 1's shape — a boundary that refuses on one side and approximates on the other, no stated reason |

**Order:** C1 → C2 → C3 first. C1 and C2 are the two that commit wrong numbers to
snapshots, and C3 makes an existing documentation claim true for one lookup.

**C1 and C2 shipped together (2026-08-05)** — they were one fix wearing two hats.
C2's shared `_align_to_spine` helper is where C1's tz coercion belongs, so doing
C1 alone would have meant writing the guard twice and deleting one. Both failure
modes have tests that fail against the previous code.

**C3 shipped (2026-08-05).** Worth recording what it cost, because it was
advertised as a one-line fix and the line itself was: the work was re-pinning a
golden test that had pinned the *wrong* numbers, and noticing that the same bias
sat in the two-level `decomposition` and the `interaction` row. A golden test
that pins values without also asserting the property those values exist to
protect will happily lock in a bug — the re-pinned test now asserts the
reconciliation directly. *(C4 shipped 2026-08-13; see its row.)*

**Order for the open remainder (2026-08-17):** C12, C23, C24 and C25 went
first — the ones on the path of a real client's ordinary RCA — and all four
shipped the day they were filed ([milestone report](milestone_readiness_2026_08_17.md) §3);
C9 and C13 followed the same day, and C7 closed the list once the author chose
option 1 on the ratio-centre decision. **The gate is closed.**

**C10 shipped (2026-08-08), and cost two new rows.** It was filed as a
data-authoring item and it was mostly that, but the row predicted it would
"surface further engine gaps while doing it" and it did: C11 and C12 are both
findings from authoring the tree, not from reading the code. Neither could have
been seen without a tree that actually declares `kind`, `dimensions` and
`expected_signs` — which is the argument for keeping the reference tree honest
in the first place. Two things worth recording. The audit's count of 31 rates
was low by 15: a name heuristic catches `_rate` and `_ctr` but not
`average_new_mrr`, `page_speed` or `sent_emails_per_contact`, and the mean of a
month of daily averages is no more meaningful than the mean of a month of daily
conversion rates. And the grain cut is not a per-node choice — a rate parent may
not be finer than its child, so declaring one node's grain propagates through
its whole downstream chain, and since weeks do not nest in months a tree with a
single apex admits `{day, week}` or `{day, month}`, never all three.

*(This horizon is a punch list with an end, so rows are added only for defects
found while closing it, never for new scope. C11–C14 were found while closing
it. C15 and C16 are the exception that proves the rule: they are not new scope
either, but they were not found while closing this horizon — they came from a
second hostile review (2026-08-12) aimed at the release gates, and they land
here because they are the same failure class the horizon exists for, a
plausible wrong number at the provider boundary. Both block the first client
deployment; C15 also blocks PyPI.)*

*(**C17 and C18 come from the same review and the same exception** — but from
its second pass rather than its first. The review was frozen at `c18d150` and
said so; re-checking all 33 findings against `e433daa` on 2026-08-12 confirmed
28 still live, and promoted these two out of the "high" and "medium" tiers into
this horizon on the horizon's own test — does it hand someone a number the
engine cannot defend. C17 does, over MCP, where the NaN becomes `null` and the
surviving numbers are arithmetically impossible; C18 does directly. **The other
26 findings are not in Horizon 0 and should not be moved here**: they are
availability, performance, packaging or documentation defects, and the ones
fixed in the same pass are recorded in [2.18](#horizon-2--make-it-repeatable-a-stranger-can-onboard).
The triage that made those calls, with the reproductions behind them, is
[`archive/grill_2026_08_12_triage.md`](archive/grill_2026_08_12_triage.md)
— archived 2026-08-17, its three then-open findings carried forward as
[C25](#horizon-0--correctness-numbers-the-engine-cant-defend)(b) and
[2.20](#horizon-2--make-it-repeatable-a-stranger-can-onboard).)*

*(**C19–C22 are the read-the-numbers exception** — found 2026-08-13 by a
reviewer running the demo trees and reading the output rather than the code
(PR #66), which is the practice the fifth rule prescribes and the one both
hostile reviews stopped short of. All four shipped the day they were found;
the rows exist because [1.11](#horizon-1--prove-it-a-trustworthy-reproducible-rca)
already cited C21 as a design lesson and a punch list a row cites had better
carry the row. **C23–C25 are the 2026-08-17 audit's exception** — the
[milestone-readiness audit](milestone_readiness_2026_08_17.md) traced four
recently-decided policies from decision to code to tests and found the same
failure class at three boundaries the four rules' structural tests do not
enumerate: engine→MCP shaping, engine→`app.js`, and metric-path→slice-path.
Its other findings — mediums and lows that are test gaps, stale comments or
doc drift rather than wrong numbers — stay in the report and are not rows.)*

**Exit:** every row ✅, and no statement in [`docs/model.md`](../docs/model.md) or
the [white paper](statistics_whitepaper.md) describes behavior the code does not
have.

---

## Horizon 1 — Prove it: a trustworthy, reproducible RCA

Goal: an RCA a stakeholder believes, on governed metrics, that re-runs deterministically.

| # | Item | Status | Why |
|---|------|--------|-----|
| 1.1 | **Statistical hardening finish** (T9) — input validation, seasonality identifiability, and the example's documented pitfall. Shipped: `_validate_windows` (ordering; overlap is an error) + `_validate_coverage` (snapped windows must lie fully inside the node's own data; lagged parents checked on their *shifted* windows and reported with parent/lag/shifted dates) on both `run_rca` and `shapley_attribution`; a gap-free-spine check in `build_grained` naming up to 10 missing periods, plus an inner-join drop warning; Nyquist-filtered Fourier harmonics (`identifiable_harmonics`: keep harmonic `k` only when `2k < period`, so periods 3–4 keep one and `period < 3` is rejected at parse time) with dropped harmonics reported in `seasonality_warnings`; annual component removed from the jaffle example and the B2B MRR reference tree | ✅ | Silent-corruption guards. Every case fixed here produced a *plausible wrong number* rather than an error: a partly-covered window averaged whichever periods existed, a hole in the date spine shifted every downstream date (positional `t`, lags, bootstrap blocks), and periods 2–4 fit rank-deficient seasonal designs whose extra parameters were pure prior |
| 1.2 | **Calibration test suite** (T10) — known-root-cause recovery, null-case restraint, CI coverage against synthetic ground truth. Shipped as `tests/test_calibration.py` (contemporaneous/lagged/identity recovery, null + unrelated-parent restraint, 20-world CI coverage), made deterministic by the sampler seeding | ✅ | The moat made testable; guards T1–T9 against regression |
| 1.3 | **Config hardening** — per-metric grain floors, `kind` (flow/stock/rate) and sign-convention metadata. `grain` + `kind` shipped with 1.7; `expected_signs` (declared coefficient direction + contradiction diagnostic) shipped after a live wrong-sign what-if on the Net-New-MRR tree; display sign conventions shipped as `direction: up_is_good\|down_is_good\|neutral` (goodness-aware UI coloring; arrows stay directional) | ✅ | Table stakes before config lands in an external repo; prevents cumulative-vs-flow and sign traps |
| 1.4 | **UI trust finish** — fit provenance in the Metric tab, name-keyed coefficients, fit-window controls (U5); accessibility & keyboard pass (U6) | ○ | The reader/reviewer persona is the audience these features serve |
| 1.5 | **Exportable RCA report** — one click → self-contained HTML (printable to PDF): target strip, tree snapshot, ranked causes, attribution tables, methods footnote. Shipped client-side from the Share menu (embedded PNGs, zero external requests) | ✅ | The shareable artifact; the thing an analysis becomes when it leaves the app |
| 1.6 | **Validate against a known incident** — replay a historical anomaly on real governed data end-to-end. Done on the Net-New-MRR tree: the May→June 2025 credit-pack→unified-subscription migration recovered live (expansion MRR ≈ the whole swing), on both the daily and weekly engines | ✅ | The validation moment: recovering a known answer earns trust on the unknown ones |
| 1.7 | **Per-node aggregation grain** — let a node declare its natural grain (daily flows, weekly/monthly cohort rates); resample each node to its grain before fit/attribution instead of forcing daily. Extends 1.3's `kind`/grain metadata; pairs with a two-level attribution view (window-aggregate identity as headline, per-day covariance as drill-down). Spec below; design + research in [`grain_design.md`](grain_design.md) / [`grain_research.md`](grain_research.md). Shipped in full: `grain`/`kind` schema, native-grain providers, per-grain storage, grain-aware fit/RCA/simulate with window snapping, cohort-aligned lagged identities (`formula` + `lags`), and the two-level attribution view (headline means-bridge with explicit co-movement row as the RCA default; per-parent detailed split as drill-down) | ✅ | Ratio/cohort nodes are degenerate at daily grain (ARPU on a 1-member day; conversion on a low-volume day), producing noise the bootstrap then papers over. Motivated by the New-MRR sub-tree build — see [`authoring_deterministic_decompositions.md`](authoring_deterministic_decompositions.md) |
| 1.8 | **Covariance-asymmetry test + fix** — per-day Shapley drops reference-window covariance from the reconstruction baseline, so `unexplained` absorbs `−cov_ref` for co-moving multiplicative factors even on exact identities. Add a characterization test, then symmetrize the baseline. Spec below. Shipped as a three-part symmetric decomposition (`φ = φ_means + φ_cov_an − φ_cov_ref`, the roadmap's option (b)): both windows evaluated per-day, `unexplained` = measurement residual only, per-parent parts exposed as `decomposition` in `GET /shapley` | ✅ | Attributions on multiplicative nodes with correlated factors under-explain by exactly the factors' reference covariance — violates "never ship a number the engine can't defend" (principle 3) |
| 1.9 | **A stock-accumulation edge** — express `level[t] = level[t-1] + inflow[t] − outflow[t]` (subscriber base, customer count, cash) as a first-class relationship. It is an identity, so it wants exact attribution like any other `formula` node, but it is a *self*-reference: written as a formula it is a self-loop and the DAG rejects it. Needs a distinct edge type carrying the prior period's own value, plus a decision on what its Shapley decomposition attributes to (the opening level, or only the period's flows) | ○ | Surfaced by C10. The reference tree wanted `total_email_subscribers[t] = base[t-1] + new[t] − unsub[t]`; with no way to say it, the previous author wrote `new − unsub` — the period's **net add** — and then multiplied it by send frequency, a quantity that goes negative on any day with net unsubscribes. C10 resolved this by dropping to a measured leaf stock and deleting the two flow nodes, which is correct but costs the whole explanation: the tree can no longer say *why* the base moved. Stock-and-flow is the most common shape in SaaS finance after the waterfall, and today the engine can only model it by discarding it |
| 1.10 | **Reference-window defaulting + history discovery** — make the reference window optional everywhere (engine, API, MCP, UI): omitted references default to the *matched adjacent block* (4× the analysis length, min 28 days, whole-week length when seasonality is in the target's scope, clamped to loaded data), echoed back with `reference_defaulted`. Surface per-node fit-window length and `seasonality_warnings` in RCA output. Add `BaseDataFetcher.earliest_date` discovery (`/meta.earliest_available`, doctor "history headroom", UI nudge to widen `--start-date`). Spec in [`reference_window_design.md`](reference_window_design.md) | ✅ | Users conflate the reference window with the training window and hand-shrink both: the fit already uses all loaded history before `analysis_start`, and the reference is only the comparison baseline — where "as long as possible" is actively wrong on a trending metric (the gap becomes "current vs long-run average" and trend absorbs it). Nothing said either thing, and nothing could say "more history exists upstream of `--start-date`" |
| 1.11 | **Rates that are undefined, not zero** — a rate whose denominator is legitimately zero in some periods has no value there, and the engine had no way to say so. `_align_to_spine` refused to gap-fill a rate, correctly (filling `0` asserts "the average was zero"; forward-filling asserts last period's rate applied) — but the refusal took the whole tree down, so the metric could not be served at all. Shipped in three parts. **(a) `source` is optional on a `formula` node**, and its presence is the switch: with one the node is fetched, its identity is checked against the fetched series **at load** (not only inside an analysis window), and `unexplained` means *the identity missed by this much*; without one the node is derived and `unexplained` is **0 by construction**. The two zeros are distinguishable everywhere the number appears — `unexplained_status: measured\|definitional` in the payload and over MCP, *unexplained — none by definition* in the RCA table, and a written-out caveat in the export, since a static report has no hover. A derived node may not declare `bind`, `sql`, `dimensions` or `lags`; each is refused by name (`lags` because deriving period *t* from parents at *t−lag* leaves the first periods of any window underivable). **(b) A rate declares its `denominator` at node level**, derived where unambiguous (a `num / den` formula, an agreeing `dimensions[].weight`, a `bind` ratio naming a tree metric) and warned about — never refused — where it is absent, since 43 of 52 rate metrics across this repo's trees declared one nowhere. `dimensions[].weight` now defaults *from* it, so there is one source of truth; `Parser.rates_without_denominator` is the inventory, named in the startup log and reported by `doctor`. **The three shipped trees were then swept (2026-08-13) and the count is now 8 of 52, all in the reference tree** — White Cube and jaffle declare a denominator on every rate. Sources, per node: White Cube's five come from the backing dbt project's `semantic_manifest.json`, which defines them as MetricFlow `ratio` metrics or as `num / nullif(den, 0)`; jaffle's `average_order_value` and 32 of the reference tree's come from the tree's own arithmetic, where a `count = base * rate` formula makes `base` the denominator. **The remaining 8 are deliberate, not a backlog**, so `tests/test_reference_tree.py` now pins them **by name** rather than by count: four mean durations whose cohort the tree does not carry (`time_to_lead_response`, `time_to_trial_activation`, `time_to_sent_new_business_proposal`, `new_business_opportunity_sales_cycle`), three ratios over a base that is not a metric there (`prospect_coverage`, `time_on_site`, `website_bounce_rate`), and `page_speed` — a **median**, which is not `Σnum / Σden` for any pair of series, and so the one rate here that could not have a denominator under any authoring. Each carries its reason in the YAML beside the node, and the tree's header has a DENOMINATORS block so the question is settled rather than re-litigated. **(d) The third state shipped: `no_denominator: "<reason>"`** (2026-08-13). Mandatory would have been sound if the remainder were zero; it was instead a set of rates that legitimately have none, so the fix was to make *having none* sayable rather than to force one. **A separate field, not a sentinel on `denominator`** — measured against PyYAML, `denominator: none` is the string `'none'`, `null`/`~`/empty is `None` (indistinguishable from unset) and `denominator: no` is the **boolean `False`**: three near-identical spellings, three meanings, and this repo already carries that scar (the binding join key is `key` and not `on` because `on:` parses as `True`). **And a plain field, not `model_fields_set`** — that does not survive `model_dump()`, which is how [C21](#horizon-0--correctness-numbers-the-engine-cant-defend)'s `direction` default reached the browser as a declaration; verified over `/dag`. Presence is the answer, the value is the reason, and an empty reason is refused because the reason is the whole point: it is what stops the question being re-opened, and it is what `doctor` and the payload quote. Refused beside a declared `denominator`, on a non-rate, on a rate declaring `dimensions` (blending slices needs a weight per slice), and when a derivation source names a denominator anyway — those sources are *evidence* and this field is a claim about the evidence, so the contradiction is the author's to resolve rather than the parser's to silently pick a winner in. `Parser.rates_without_denominator` split into `rates_denominator_unanswered` (the lint) and `rates_denominator_none` (name -> reason), because one list could only be read as the weaker of the two facts. **The fallback is now labelled where the number is read, not only in the log**: every rate node in an RCA response carries `window_aggregate` — `components`, `period_mean_none_exists`, `period_mean_undeclared` or `period_mean_weights_unavailable` (the fourth because a declared denominator whose series misses the window silently fell back, and labelling that `components` would have the payload misdescribe its own arithmetic) — plus `window_aggregate_reason`. The UI puts it in the *label* under the baseline → actual line ("period means — no component aggregate exists"), not a tooltip, because the export has no hover; `doctor` passes and quotes the reasons instead of advising the impossible. This also fixed the smaller honesty defect the sweep exposed: `doctor` had been telling the author to "Add `denominator: <metric>`" to all eight, which for a median cannot be done. **On whether the field can now become mandatory: the trees are ready, the schema change is not free.** All four shipped trees now answer for every rate — the sweep missed `breakdown/examples/cold_start_tree.yml`, whose `avg_price` is the denominator of `mrr = paying_customers * avg_price` and now declares `paying_customers` — and `tests/test_project_invariants.py` holds the unanswered count at zero so it cannot drift back. Requiring it of *other people's* trees is a breaking YAML-schema change, which the CHANGELOG's pre-1.0 contract puts in a minor bump, and it would reject a tree that is merely unfinished rather than wrong. It is the author's call, deliberately left open. **(c) A window's rate recomputes from its components** — `Σnum / Σden`, i.e. the denominator-weighted mean of the per-period rates, so a zero-denominator period drops out of both sums and the window rate stays defined where the per-period rate is not. Each consumer has a stated policy in [`docs/model.md`](../docs/model.md#rates-over-undefined-periods): the window aggregate excludes it, `_check_contiguous` is unaffected (contiguity is about dates, and the row stays), the fit **refuses** rather than imputes (`fit_failed`, periods named — dropping the row would re-date every later period), a formula node reading the rate reports `attribution_failed`, and a window with no defined period at all gets the new `undefined_over_window` status. The bootstrap needed nothing new: its existing non-finite-replicate withholding is the policy. **Note the row's original framing was wrong in one place** — it said `formula: "a / b"` is used by zero metrics and so the "defaults from a simple ratio" path had never fired, which is true of the shipped trees but is now the documented remedy's own shape, and that shape needed its **own** Shapley branch: for `kind: rate` with `formula: num / den` over its declared denominator the window value *is* the formula of the window aggregates, so the decomposition is the window-means bridge alone and the co-movement terms are **absent** from the payload rather than reported as zero (`aggregation: "components"` on `/shapley`) **The case for leaving it optional — and for putting the gate in `doctor` rather than the parser — is written up in [`rate_denominator_policy.md`](rate_denominator_policy.md), and its recommendation shipped as [1.12](#horizon-1--prove-it-a-trustworthy-reproducible-rca).** | ✅ | **We were not inventing a representation for this; we were failing to receive one that upstream already sends.** The White Cube dbt project writes its rates as `num / nullif(den, 0)` — the canonical MetricFlow idiom — so the semantic layer returns NULL for an undefined rate, exactly right, and breakdown refused it. Two systems each individually correct and jointly broken. Measured on the demo after the fix: all 9 of `churn_arpu`'s 113 undefined weeks are `0/0` (denominator zero — a fact), 0 are missing values, and the classification is only possible *because* (b) landed. The exposure generalizes past this tree: a rate over a **stock** denominator is safe (`customer_churn_rate`: 0 undefined of 113), a rate over a busy **flow** is usually safe (`new_arpu`: 0 of 113), and a rate over a *quiet* flow is not. The finer the grain the worse it gets, so a client on daily data hits it immediately. The consequence was larger than one tree: **the demo had been un-regenerable since C2 landed on 2026-08-05**, invisible because the snapshots are committed. Distinct from [S20](#statistical-rigor-s--a-standing-workstream), which is the same series shape from the other side: S20 is a likelihood for a series that is genuinely **zero**, this is a representation for one that is genuinely **undefined** |
| 1.12 | **`denominator` stays optional; the enforcement moved to `doctor`** — [`rate_denominator_policy.md`](rate_denominator_policy.md)'s recommendation, shipped rather than left as a written argument. Two parts. **`breakdown doctor`'s "rate denominators" check now `fail`s** (non-zero exit) on any rate answering neither `denominator` nor `no_denominator`, where it previously `skip`ped — the parser is unchanged and still loads such a tree, since a hard stop at load time is the wrong place for it, but `doctor` is the surface every failure path in the product points a user at, and *"can I trust this?"* is the question it exists to answer; a window value computed by the wrong arithmetic with nothing saying so is what a `skip` here was quietly allowing to pass. Verified against all four shipped trees (none newly fail — 1.11's sweep already answers every rate in each) before landing. **`dbt_bridge._translate_derived` now recognizes `num / nullif(den, 0)`**, MetricFlow's canonical safe-division idiom, rewriting it to plain `num / den` before translation instead of refusing the whole metric — closing the narrower of [2.19](#horizon-2--make-it-repeatable-a-stranger-can-onboard)'s two findings (the ratio-expression shape; `fill_nulls_with` is the larger one and is still open). Once rewritten, the metric becomes an ordinary `formula: "num / den"` rate node and needs no denominator-specific code of its own: `parser.py`'s existing `check_rate_denominator` already derives `.denominator` from that formula shape, the same machinery a hand-authored `formula: "a / b"` node goes through — confirmed with an end-to-end test running bridge output through the real `Parser`. Anything short of this exact idiom (a non-zero guard, a different function, extra arithmetic) is still refused by name, per rule 1: the boundary refuses rather than approximates | ✅ | Closes the loop the policy doc opened the same day: permissive-and-disclosed only holds up if the disclosure actually gates something, and until this shipped, an unanswered rate's only consequence anywhere in the product was a log line and a `[SKIP]` nobody's exit code noticed. The `nullif` fix is the doc's own highest-value, smallest move — it was framed as "derive it in the bridge," but tracing the code showed the derivation already existed for the `ratio` metric type; the actual gap was that the `derived` metric type's equivalent, safer idiom was being refused outright, before it ever reached that derivation |

**Exit:** a stakeholder accepts an RCA finding; the same RCA re-runs deterministically from a fresh clone; report export exists.

### Specs for 1.7–1.8

Both came out of building a deterministic `new_mrr = new_members × new_member_arpu → new_members = non_trial_conversions + conversions_from_trial → conversions_from_trial = trial_starts × trial_to_member_conversion_rate` sub-tree against the `warehouse` provider (the [authoring lessons](authoring_deterministic_decompositions.md) doc is the companion).

#### 1.7 — Per-node aggregation grain

**Problem.** Grain is hardcoded daily: `data_fetch.py` reindexes every series onto a daily spine (`reindex(full, fill_value=0.0)`) and `MockDataFetcher` notes "Only daily grain is supported." Two consequences:

- **Ratio/cohort nodes are degenerate at daily grain.** A per-day rate factor (ARPU = daily_mrr/daily_members, conversion = daily_converts/daily_starts) swings wildly on low-volume days; the *product* survives (the count factor is also small) but the per-day *attribution to volume vs rate* is high-variance, which is why the block bootstrap is load-bearing rather than a nicety.
- **Deterministic lagged identities are disallowed.** The parser rejects `formula` + `lags` ("a formula is contemporaneous"). But `conversions[t] = trial_starts[t−k] × cohort_rate` is an exact, Shapley-decomposable identity; forbidding it forces either a contemporaneous *blended* (cross-cohort) rate or a fully probabilistic BSTS edge, with no exact deterministic middle.

**Design sketch.**

- Add an optional per-node `grain: day|week|month` (or fold into 1.3's node metadata). Each `BaseDataFetcher` resamples its series to the node's grain (aggregate-up only; downward disaggregation is undefined and should be rejected). Counts/flows sum; declared rates recompute from their components at the coarser grain, not by averaging daily ratios.
- For a formula node, all parents must share — or be resampled to — the node's grain so the identity holds *at that grain*. RCA aligns nodes in scope to a common comparison grain (coarsest in scope, or explicit) before the Shapley/posterior step.
- **Two-level attribution.** Make the window-aggregate identity the default headline (means/totals with an *explicit* interaction term — the standard price/volume/mix bridge), and keep the current per-day within-window covariance capture as an opt-in drill-down. Today that covariance detail is baked into the headline whether or not the reader wants it (and see 1.8).
- Optionally relax `formula` + `lags` to allow **cohort-aligned deterministic identities** (lagged products), so trial→member conversion has an exact deterministic form.

**Open questions.** Seasonality periods are grain-relative (`period: 7` is weekly on a daily series, meaningless monthly) — validate period against grain. Interacts with the snapshot store (2.4), which already keys on `(metric, window, grain)`. Mixed-grain trees (daily flow under a monthly rate) need a clear resample-up contract.

#### 1.8 — Covariance-asymmetry test + fix

**Diagnosis (in `engine/rca.py`).** `shapley_attribution` builds the reconstruction baseline from reference-window **means**:

- `ref_means[p] = window_mean(frame, p, ref)` (scalar per parent); `baseline = _eval_scalar(formula, ref_means)`.
- `daily_actuals[p] = _window_values(frame, p, an)` (daily); `actual = eval_formula(formula, daily_actuals).mean()`.
- `compute_shapley` is fed `{p: full(n_days, ref_means[p])}` as baselines, so `Σφ = actual − baseline`. The bootstrap path (`boot_baselines = ref_vals[p][ref_idx].mean(axis=1)`) shares the same means-only baseline, so CIs inherit the asymmetry.

`run_rca` then takes the node's **own** series gap and subtracts the formula gap: `unexplained = gap − sh["gap"]`, with `gap = window_mean(node, an) − window_mean(node, ref)`.

For an exact per-day product node `target_d = B_d·C_d` (population covariance `cov = mean(B·C) − mean(B)·mean(C)`, i.e. `ddof=0`):

```
gap        = (B̄_an·C̄_an + cov_an) − (B̄_ref·C̄_ref + cov_ref)      # node's own series, both windows carry cov
sh["gap"]  = (B̄_an·C̄_an + cov_an) − (B̄_ref·C̄_ref)                 # baseline uses means only → cov_ref dropped
unexplained = gap − sh["gap"] = −cov_ref(B, C)
```

So a mathematically exact identity yields `unexplained = −cov_ref` and attributions summing to `sh["gap"]`, which overshoots the true gap by `cov_ref`. The intended feature — attributing *analysis*-window covariance to the parents — is fine; the bug is that the *reference* window is collapsed to means, breaking efficiency against the node's own gap.

**Characterization test** (calibration suite / new `tests/test_shapley_covariance_asymmetry.py`; no provider — build the frame in memory):

1. 60 daily rows: reference = first 30, analysis = last 30. Fix means `μB, μC` (e.g. 10, 5). Construct each window as `B = μB + s·d`, `C = μC + s·d` where `d` is a fixed **zero-mean** pattern (e.g. `[+1,−1]*15`), so adding `s·d` leaves the window mean exactly `μ` while giving `cov = s²·var(d)`, tunable via `s` independently per window. Set `A = B * C` elementwise (clean identity — no grain leakage confound).
2. DAG: leaves `B`, `C`; formula node `A = "B * C"`, `parents: [B, C]`. `data` = DataFrame(date, A, B, C); `traces = {}`. Build via the existing test DAG helper (small YAML through `Parser`, or `MetricDefinition` objects directly — follow current test conventions).
3. Assertions against **current** behavior:
   - **Efficiency vs formula gap:** call `shapley_attribution(...)` directly; assert `abs(sum(φ_p.mean() for p) − sh["gap"]) < 1e−9`.
   - **Leak equals reference covariance:** `cov_ref = np.mean(B_ref*C_ref) − np.mean(B_ref)*np.mean(C_ref)`; from `run_rca`'s node-A output assert `abs(unexplained − (−cov_ref)) < 1e−6`.
   - **Zero-gap-nonzero-attribution:** set `cov_an == cov_ref == K ≠ 0`, means equal. Assert `abs(node_gap) < 1e−9` while `abs(sum(attributions) − K) < 1e−3` and `abs(unexplained + K) < 1e−3` — the node didn't move yet each factor gets a nonzero attribution, cancelled by an equal-and-opposite `unexplained`.
   - **Linearity sweep:** for `K_ref ∈ {0, 0.5, 1, 2, 4}` (means and `cov_an` fixed), assert `unexplained ≈ −K_ref` (fit slope ≈ −1, intercept ≈ 0 within tol).

**Fix + acceptance.** Symmetrize the baseline so the reference window is treated per-day like the analysis window — e.g. `baseline = eval_formula(formula, {p: _window_values(frame, p, ref)}).mean()`, and give `compute_shapley`'s non-members their reference **daily** values rather than the scalar mean. Windows of unequal length have no positional pairing, so either (a) require equal-length windows for exact per-day pairing, or (b) reframe the game to attribute the covariance **delta** `(cov_an − cov_ref)` and route only genuine model error to `unexplained`. Preserve the analysis-window covariance-capture feature; keep determinism (`rng` seed 0) and the bootstrap CI structure. After the fix, flip the tests to the target behavior:

- means equal & `cov_an == cov_ref`: `sum(attributions) ≈ 0` and `unexplained ≈ 0` (nothing moved, nothing attributed);
- means move, cov equal: attributions recover the mean-driven contributions, `unexplained ≈ 0`;
- cov moves, means equal: `sum(attributions) ≈ cov_an − cov_ref` (the honest covariance-shift signal), `unexplained ≈ 0`.

---

## Horizon 2 — Make it repeatable: a stranger can onboard

Goal: onboarding a new tree costs a day, not a week.

| # | Item | Status | Why |
|---|------|--------|-----|
| 2.1 | **Connection doctor** for dbt Cloud SL — walk the auth chain (token → host cell → environment → SL config → credential → mapping), name the missing link, emit a copy-paste remediation page for the admin steps outside our control. Shipped as `breakdown doctor --tree …`, broader than scoped: covers warehouse (auth mode → CLI → profile → connection → per-metric SQL via the real `fetch_metric`), cloud (one `client.metrics()` call proves the whole SL chain), and local; all checks run in one pass with copy-paste remediation. Landed with the deployment kit (installable CLI, Docker image, degraded startup + `/health`) | ✅ | Turns days of provisioning archaeology into minutes; is itself the onboarding demo |
| 2.2 | **CSV ingest + per-metric provider mixing** — a tree where some nodes come from the SL and some from direct SQL/CSV is a normal migration state (`source:` already carries a provider-qualified path). **Amended (2026-08-10): promoted from incremental convenience to foundational**, and its mechanism is now named — the per-node `bind:` block of [2.9](#horizon-2--make-it-repeatable-a-stranger-can-onboard), with `provider:` demoted to a tree-level default. Mixed *sources* is in scope; mixed *bound/unbound* nodes are not, and inherit the partially-fitted-path problem that removed 2.7 (see [`semantic_layer_connectivity_design.md`](semantic_layer_connectivity_design.md) §3). **Amended (2026-08-11): inherits the local-file on-ramp from the removed 2.12.** The mechanism already exists — a `bind:` block over `read_parquet('…')` or an inline `bind.sql`, executed by the DuckDB connector — so what remains is a provider type that reaches per-node bindings with no dbt project on disk | ◑ | direct-SQL/warehouse provider exists; CSV ingest and per-metric mixing remain. The zero-integration on-ramp: "send a CSV, get an RCA," then migrate node-by-node to governed metrics. Tree-level `provider:` forces an all-or-nothing choice at exactly the moment a client is least able to make one |
| 2.3 | **Tree scaffolder / bootstrapper** — enumerate SL metrics; turn `derived`/`ratio` `input_metrics` into formula edges; LLM-assisted import of latent trees (canvas exports, metric docs → draft YAML). **Amended (2026-08-10), twice over.** *(a)* The semantic-layer substrate is identified and it is not dbt Cloud: `target/semantic_manifest.json` carries names, descriptions and `ratio`/`derived` input metrics (literally formula edges), `agg` and metric type infer `kind`, `time_granularity`/`agg_time_dimension` infer `grain` — blocked on [2.10](#horizon-2--make-it-repeatable-a-stranger-can-onboard), not on a Metadata API subscription. *(b)* **Extend it past clients who have a semantic layer at all:** read the warehouse schema plus whatever SQL already exists (dbt models, BI dashboard queries) and draft the tree *and its [2.9](#horizon-2--make-it-repeatable-a-stranger-can-onboard) bindings* for human review | ○ | Blank-YAML is the adoption killer; trees already exist in fragments. The inferred `kind`/`grain` is exactly the metadata [C10](#horizon-0--correctness-numbers-the-engine-cant-defend) found missing or wrong across 46 nodes of the reference tree — a scaffolder emitting it is worth more than one emitting structure alone. And *(b)* is the difference between a six-week prerequisite and a thirty-minute onboarding for the no-semantic-layer client, which is the largest single segment. It is only tractable because 2.9 makes the generation target twelve reviewable lines beside the node they serve, rather than semantic models in a foreign dialect with cross-references |
| 2.4 | **Snapshot store** (parquet/DuckDB) — fetch once per (metric, window, grain), refit from snapshots. Shipped as a parquet read-through cache at the fetcher boundary (`snapshots.py`): tree-adjacent `.breakdown/snapshots/` (committable → RCAs re-run from a fresh clone), `--refresh`/`--no-snapshots`/`--snapshot-dir` controls, failure-soft on read-only mounts, and a warehouse outage is survivable when every metric has a snapshot. DuckDB deferred until scheduling (3.1) needs cross-snapshot queries | ✅ | Reproducibility, provider-migration invisibility, warehouse politeness, and the foundation for scheduling |
| 2.5 | **MCP server** — expose `run_rca`, `get_tree`, `explain_metric` as tools | ✅ | AI analysts guess at "why"; breakdown is the grounded causal tool they should call. Cheap (endpoints exist), differentiating, and meets users where they already ask why-questions. Shipped as streamable HTTP at `/mcp` with a fourth tool beyond the original scope (`run_whatif`); analysis responses carry `how_to_read` caveats and `report_url` deep links into the UI |
| 2.6 | **Outsider docs pass** — install guide + first-tree tutorial on public data, and a README that is a landing page rather than a manual. Planned in three stages, of which this row owns two. **Stage 1 shipped 2026-08-13:** the README was 1,707 lines / 122 KB and **71% of it was reference and operations** — the YAML reference alone was 34%, the API reference 24% — while the part that makes a stranger care ("two kinds of causal relationships") was 4%. That file is also the wheel's `METADATA` long_description, so it *is* the PyPI landing page. The two reference blocks moved to [`docs/yaml-reference.md`](../docs/yaml-reference.md) and [`docs/api-reference.md`](../docs/api-reference.md), leaving an orientation and a link; `Deploying` was reordered out of the way (a newcomer was being told how to deploy before how to author a tree) but deliberately **not** moved. README 1,707 → **769 lines**. The load-bearing part was not the move: `tests/test_readme.py` scanned one file, so relocating the examples would have silently deleted the guard built after four separate drift findings — it is now `tests/test_docs_examples.py`, parameterized over a **list** of docs, failing with the filename and line (`[yaml-reference.md:L299]`), with the route-table and skip-list assertions following per file. **Stage 2 shipped in pieces, finishing 2026-08-17:** `Deploying` → [`docs/deploying.md`](../docs/deploying.md) and the install guide (README) landed earlier; [`docs/first-tree-tutorial.md`](../docs/first-tree-tutorial.md) is the last piece — empty file → running RCA in about half an hour, **building the bundled jaffle tree from scratch** so the finished artifact is the file that ships in the wheel and every step has a known-good answer beside it. One deliberate narrowing of this row's original wording: it said "on public data", and the tutorial runs on the deterministic `mock` provider instead, because the only zero-credential real-data path would be a dbt project (a six-week ask for a first half hour) and the tutorial's whole point is that its every command runs anywhere — the real-data swap is its own section at the end. The load-bearing part, again, was the harness: the tutorial is an entry in `tests/test_docs_examples.py`'s `DOCS` list, so all 7 of its YAML blocks parse lint-free (excerpt parents stubbed and named) and both its curls replay against the exact tree and window it tells the reader to load — a tutorial whose commands the suite has already run. Writing it caught one escape in its own guard: an example fenced *inside an indented bullet* is invisible to the block extractor, which anchors at column 0; the block was flattened rather than the regex loosened. Stage 3 is [3.9](#horizon-3--make-it-findable-and-sticky-it-comes-to-you) and is gated separately, on purpose | ✅ | First impressions for anyone arriving cold — and the first impression is 122 KB of reference material on PyPI. The staging is the point: stages 1 and 2 are content moves with a bounded payoff, while a docs *site* is infrastructure (a generator, a CI job, hosting) that would otherwise ride along inside a nearly-finished content item |
| 2.8 | **Dimensional slicing on the `warehouse` provider** — the SQL contract for a sliced fetch (`fetch_metric_sliced` returning `[date, slice, value]`), `doctor` checks for it, and the docs. Promoted out of [3.2](#horizon-3--make-it-findable-and-sticky-it-comes-to-you)'s "Remaining" because it is not a finishing touch — it is the feature carrying the thesis, missing on the one provider that needs no dbt SL | ○ | Slicing is the bridge from *metric* to *event*: "AOV fell" is a narrowing, "AOV fell, concentrated in EMEA on iOS 18.4" is a diagnosis — and the diagnosis is what the customer was going to spend the next three hours on. `tree × slice` is the biggest pure-product differentiator (3.2), and it currently works on `mock` and dbt SL only, while 3.3 exists precisely as a hedge against thin dbt-SL adoption. That gap is worth more than the rest of this horizon |
| 2.9 | **Per-node binding contract** — a node binds to its source individually via a `bind:` block (`relation` or inline `sql:`, `grain_key`, `time_column`, `agg`, `numerator`/`denominator` on ratios, `entity_key` on non-additive, `dimensions` with many-to-one join paths); `provider:` demotes to a tree-level default, so existing trees are unchanged. Carries the **grain-claim assertion** — `doctor` asserts `count(*) == count(distinct grain_key)` on every resolved relation and fails the binding when they disagree — and refusal discipline: a ratio with no declared denominator, or a `count_distinct` with no `entity_key`, is a `doctor` error rather than an approximation. Hand-written bindings that shadow an importable definition also fail `doctor`, with a per-node `override: true` escape. **Amends the "our own metric definition language" non-goal**; the scope boundary and the `sql:` stop rule that holds it are in [`semantic_layer_connectivity_design.md`](semantic_layer_connectivity_design.md) §4.1 **Shipped:** `bind:` on the node with `relation`/`sql`, `grain_key`, `time_column`, `agg`, `numerator`/`denominator`, `entity_key`, `dimensions`; `provider:` demoted to a tree-level default so existing trees are unchanged. The join key is `key`, not `on` — PyYAML is YAML 1.1, where a bare `on:` parses as boolean `True` and would silently drop the key. Refusals: a ratio without a denominator, a `count_distinct` without an `entity_key`, a non-additive parent finer than its child. | ✅ | The prerequisite is the deal-killer: most prospects have no semantic layer, "come back once you've built one" is a six-week tax before first value, and what they would build serves org-wide self-serve when breakdown needs one series per node. Annotation is mandatory regardless — `kind` has no MetricFlow equivalent and is not derivable from `agg` — so the real choice is a coherent contract versus a pile of per-source overrides. The grain assertion is a differentiator by itself: MetricFlow and Cube accept declared relationships on trust, so silent fan-out is a class of wrong answers they structurally cannot catch and we can |
| 2.10 | **dbt binding — read the semantic manifest, generate the SQL, prove it agrees** — parse `target/semantic_manifest.json` (originally via `metricflow_semantic_interfaces`, **never** the deprecated `dbt-semantic-interfaces`, which returns new-spec metrics with no aggregation *and validates with 0 errors* — 61/61 on a real project; the models moved in-tree in [2.15](#horizon-2--make-it-repeatable-a-stranger-can-onboard)), compile the narrow measure × grain × range × dimension query with sqlglot, execute over the project's own `profiles.yml`. **Zero new credentials, no dbt Cloud, no SL credential, no plan tier** — `dbt parse` on dbt Core writes the artifact. **Amended (2026-08-10): differential verification split out to 2.14**, which is where `--verify-against-metricflow` now lives — it could not be built honestly alongside the rest, because `mf` dies on import on Python 3.14 (dbt-core's mashumaro error, the breakage the README documents), leaving no MetricFlow to test against. v1 = simple + ratio + derived-without-offset; **the `+ filters` this row claimed until 2026-08-12 was never true** — the manifest models didn't declare `filter` and `_Node` ignores extras, so filtered metrics translated *filterless* and served the whole relation ([C15](#horizon-0--correctness-numbers-the-engine-cant-defend)). They were refused by name until [2.17](#horizon-2--make-it-repeatable-a-stranger-can-onboard) shipped filter resolution, so the coverage line now reads **simple + ratio + derived-without-offset, plus filters resolving to the measure's own relation** — a filter reaching across a join or into a time dimension is still refused by name; `count_distinct` marked non-re-aggregable and refused by `resample_up`; conversion metrics and `non_additive_dimension` diagnosed by name. Per [`semantic_layer_connectivity_design.md`](semantic_layer_connectivity_design.md) **Shipped:** `dbt_bridge` (manifest → bindings), `dbt_sql` (bindings → dialect SQL), `dbt_provider` (profiles.yml → connection → aligned series), and a six-step `doctor` chain ending in the grain claim. Verified end to end against a real Databricks warehouse. Two bugs only the warehouse could find: Spark's `trunc` returns NULL for DAY, so day grain was unusable until every expression was parsed in the target dialect; and a quoted `"date"` is an identifier on DuckDB but a string literal on Spark and BigQuery. Differential verification remains [2.14](#horizon-2--make-it-repeatable-a-stranger-can-onboard). **Extended 2026-08-11 with a BigQuery connector**, which is the shape follow-on work here takes: `ADAPTER_DIALECTS` and the ISOWEEK override meant BigQuery *SQL* had been generated correctly since this row shipped, and `CONNECTORS` was the only thing missing — so a BigQuery shop was pushed onto `local` and its `mf` subprocess (and a sidecar env, since the MetricFlow route pins dbt-core) by one absent function. Worth stating as a rule: **generation and execution ship separately here, and a mapped dialect is not a supported warehouse.** The remaining unmapped-to-unconnectable gaps are Redshift, Spark, Trino, Athena and ClickHouse. **BigQuery is still on the wrong side of that rule, 2026-08-13.** Dialect mapped since this row shipped, connector 2026-08-11, truncation corrected 2026-08-12 — and *nothing has ever executed against a real BigQuery*. Every BigQuery test in the suite is a refusal path or a monkeypatched client, and the three auth methods (`oauth`, `service-account`, `service-account-json`) are unexercised. Databricks has “verified end to end against a real warehouse” in this row; BigQuery does not, and the malformed day/month truncation is what that gap looks like when it goes unnoticed for two days. A plan to close it — free-tier account, loading White Cube, what to run in what order, and twelve failure predictions derived from this code — is [`bigquery_end_to_end_test_plan.md`](bigquery_end_to_end_test_plan.md). One small defect it names in passing: `check_provider_extra("dbt")` probes for `sqlglot` only, so a missing `google-cloud-bigquery` is reported as a *warehouse connection* failure whose remediation is `dbt debug` — which will pass. **Corrected 2026-08-12, and it is that rule's own bill coming due:** the claim above that "BigQuery *SQL* had been generated correctly since this row shipped" was **false**. `_TRUNC` emits the portable `DATE_TRUNC('<part>', col)` and `_parse_dialect` parses in the *target* dialect so nothing rewrites it — but BigQuery's signature is `DATE_TRUNC(date_expression, date_part)`, and only `("bigquery", "week")` had an override. Every **day- and month-grain** query generated for BigQuery was malformed, leaving week as the only usable grain, and `doctor`'s grain assertions failed too. Worse, the week override was wrong as well for the common case: BigQuery's `DATE_TRUNC` takes a `DATE`, and a dbt fact table's `agg_time_dimension` is typically a `TIMESTAMP`, so BigQuery had **no** fully working grain rather than one. Fixed by casting: `DATE_TRUNC(CAST(col AS DATE), PART)` for all three grains, correct for DATE, TIMESTAMP and DATETIME alike — type-aware `TIMESTAMP_TRUNC` was rejected because the type is not knowable here (`BindingSpec.time_column` is a bare string, MetricFlow manifests record no column data type for a dimension, and `_column()` may return an arbitrary expression). The window predicates still compare the raw column, so partition pruning is unaffected. **The test discipline is the actual lesson**: `test_dbt_sql.py` asserted on emitted *text*, which is exactly what let this through for the third time, so the fix ships with tests that parse the generated SQL in the BigQuery dialect and assert on the **parse tree** — plus a meta-test proving that parse genuinely distinguishes the broken form from the fixed one, and a cross-dialect guard so a future BigQuery fix cannot generalise and silently break DuckDB, Snowflake or Databricks. | ✅ | Both dbt paths we ship tax principle 4 in ways we cannot fix from our side: `cloud` dies on provisioning archaeology (2.1's whole existence) and excludes dbt Core shops entirely, while `local` shells out to `mf` once *per slice* behind a 120s timeout and is why the `dbt` extra is broken on Python 3.14. This is the `warehouse` provider with generated instead of hand-written SQL, which also kills warehouse-direct's only real objection — nothing is mirrored, so there is nothing to drift, and the differential check proves that rather than asserting it. Push-down looked like the safe answer and cannot serve this client: pushing down to dbt means dbt Cloud or `mf`, the two things being escaped |
| 2.11 | **Query provenance in the API/UI** — surface the generated SQL per fetched series and per slice; "show query" on node cards, slice panels and the RCA export **Shipped:** `GET /metrics/{name}/query` plus a `show query` toggle on the Metric tab. `sql: null` is a real answer carrying the provider's own reason — `mock` synthesizes, `local`/`cloud` never see the SQL — rather than an error. Provenance does not depend on whether a query ran: a snapshot hit serves the number without executing anything, so the response carries `executed` and generates the statement for the loaded window when nothing ran. | ✅ | Principle 3 is "never ship a number the engine can't defend", and today `warehouse` is the only provider where the user can see what was queried — because they wrote it themselves. Every other number in the product is unfalsifiable by the person being asked to trust it. Nearly free once 2.10 generates the SQL |
| 2.13 | **Supersede the `local` provider** — ✅ **Amended and shipped (2026-08-11).** Scoped as "retire", on the claim that 2.10 does everything `local` does. **That claim is false, and the amendment is the point:** `local` hands a metric name to MetricFlow, which plans the SQL, so it serves what the `dbt` provider refuses rather than approximates — cumulative metrics, offset windows, `min`/`max`/`median`/`percentile`, conversion metrics, `non_additive_dimension`. Measured on two real projects: 2 of 24 and 8 of 86 metrics. Shipped instead as a **per-tree** answer — `doctor`'s `dbt provider migration` check runs the bridge against the tree in front of it and either says every metric translates or names the ones that need MetricFlow — plus a README migration section stating the gap. `local` stays supported and tested; no removal release is named, and none should be while the gap exists | ✅ | Two providers solving one problem is a maintenance and docs cost, but a blanket deprecation would have been noise for the author whose tree genuinely needs MetricFlow, and actively misleading if the general claim were taken at face value. `cloud` is **not** superseded |
| 2.14 | **Differential verification against MetricFlow** — `breakdown doctor --verify-against-metricflow` runs our generated SQL *and* MetricFlow's own compiled SQL for the same metric, grain and window, and asserts equality on real data. Makes MetricFlow a **validation-time** dependency, never a runtime one, and pins it in CI against a fixture project. Constrained to Python ≤3.13 and the `dbt` extra, since it needs `mf`. **Acceptance criteria exist now** — §6 of [`bigquery_end_to_end_test_plan.md`](bigquery_end_to_end_test_plan.md) sets them out in three tiers, with the committed White Cube parquet as the fixed oracle: a DuckDB-computed answer for every metric × grain × window, already in git, which is the stationary side of the differential. Worth carrying over its sharpest point: **a totals-only check passes a week-bucketing bug**, because `_floor_labels` relabels a Sunday-bucketed week to the previous Monday and it lands on the spine invisibly — so the comparison has to be per-period, and has to cross a New Year, where “Monday-ish” separates from genuinely ISO | ○ | The strongest argument against generating our own SQL is that a number disagreeing with the client's dashboard is dead on arrival however right we are. Push-down was the safe-looking answer and cannot serve a dbt Core client — pushing down to dbt means dbt Cloud or `mf`, the two things [2.10](#horizon-2--make-it-repeatable-a-stranger-can-onboard) exists to escape. Proving agreement instead of assuming it is what closes the gap. Split from 2.10 rather than shipped unverified: `mf` does not import on 3.14, so there was no MetricFlow in the build environment to test against |
| 2.15 | **No runtime dependency on dbt Labs** — ✅ **shipped 2026-08-11.** The `dbt-bridge` extra pulled `metricflow` solely for `metricflow_semantic_interfaces`: twelve transitive packages and a `<3.15` Python ceiling for a single `parse_obj` call. The manifest is a *resolved* JSON artifact, so `breakdown/dbt_manifest.py` now models the ~19 fields the bridge reads and the extra is one package (`sqlglot`). `metricflow` moves to the dev group as a **differential oracle** — `tests/test_dbt_manifest.py` parses the same fixtures both ways, asserts every consumed field agrees, and asserts our reading survives MSI's own normalisation. Verified byte-identical bridge output on both real dbt projects (18/4/2 and 60/18/8) | ✅ | Two interested clients, zero dependents and nothing published yet — the cheapest this debt will ever be to cut. MetricFlow's open-source commitment has already wobbled once (AGPL → BSL → Apache) and the package was renamed under us mid-session (DSI → MSI), so inheriting its cadence was the wrong bet for a product whose pitch includes running on current Python. Unknown keys are still ignored, which is precisely how DSI returned new-spec metrics with no aggregation while validating clean — safe here **only** because the bridge hard-fails on a metric matching neither shape, which is why that refusal must never be relaxed |
| 2.16 | **Multiple metric trees** — ✅ **shipped 2026-08-12.** `--tree` accepts a directory (the `breakdown/` folder of a dbt repo), each `*.yml` a tree with its id from the filename stem. Trees are **peers** — a wide revenue tree, a team's, a feature's, one standing behind a target — and nothing prescribes a lifetime or requires a goal. The optional `tree:` block carries `title`/`description`/`owner`/`period`/`goal` (`metric`, `target`, `direction`, `deadline`), every field optional so most trees declare little or nothing and `extra='ignore'` means annotated trees already load on older builds; `goal.metric` must resolve and `goal.direction` defaults from the metric's own `direction`, disagreement being an error. Per-tree `TreeState` (`api/trees.py`) replaces the ~40 `app.state.X` reads with `BreakdownState` aliasing the **default** tree's onto `app.state`, so the whole suite passed that step untouched; per-tree lock, and the trace cap stays global via a `TraceStore` keyed `(tree_id, metric, fit_end)` with a per-tree `TraceView` speaking the engine's own key. **Lazy loading**: boot parses every tree and fetches none, so `GET /trees` is instant — except a single-file `--tree`, which still loads eagerly (lazy buys nothing with one tree), with `--eager` for a directory. Tree-scoped routes come from **one** `APIRouter` included twice (bare and `/trees/{id}`), reading the id off `request.path_params`, so the aliases cannot drift. MCP gains `list_trees` and an optional `tree` on every tool; `report_url` carries `#tree=`. UI ships the index (one flat grid of peer cards, goal progress where declared, explicit *not loaded*, per-tree error cards), the header switcher, `#tree=` parsed first, and per-tree `localStorage` keys. Designed in [`multi_tree_design.md`](multi_tree_design.md), whose goal-tree-first framing was **amended by the author on 2026-08-12** after first implementation (spec §12) — the index was regrouped from `period` to a flat grid and the copy stopped prescribing quarters. **Two further deviations, both stated in §11**: `/ui` opens the tree directly when there is exactly one (§4's "if the index is skipped" — an index of one card is a toll booth), and a goal card shows *share of target · days to deadline* rather than §7.1's "on track"/"behind", because §3 makes `period` a free-form label so the goal window's **start** is nowhere in the tree and a pace verdict would be a forecast the engine never made | ✅ | "A company can serve one **or more** trees" is the whole ask, and any of them may be durable or disposable, with a goal or without. The wide revenue tree is one lens; a marketing tree detailing channels and campaigns, a product tree about feature adoption and retention, and a tree standing behind a five-year target are others, and a focused tree can be as durable and as useful as the big one. One process per tree is the workaround today: N ports, N warm trace caches, no index, and no way to move between them. The YAML decision is the load-bearing one and is why this was specced before it was built — shipping `title:` alone would force a second, breaking change against trees that by then live in customers' repos |
| 2.17 | **Real filter support — `where:` on `BindingSpec`** — carry a predicate on the binding, fed by dbt's `where_sql_template`, and compile it into the generated SQL alongside the grain/window/dimension clauses. **Designed in [`filter_support_design.md`](filter_support_design.md)**, which corrects three claims this row made when it was filed. *(a)* There are no manifest-derived join paths to reuse: `_categorical_dimensions` emits **same-relation dimensions only**, deliberately, because proving a join many-to-one needs the joined model's primary entity — so v1 admits only references to the binding's own primary entity and skips the rest. *(b)* "What is missing is `BindingSpec` having anywhere to put one" understates it materially: `where_sql_template` is **Jinja, not SQL**, so the field is the small part and the resolver is the item. *(c)* Not "authorable directly for hand-written ones" — the spec makes `where:` **import-only**, since `bind.sql` already expresses every hand-written filter and a convenience field is exactly what §4.1's stop rule forbids; the new rule is that an import-only field must be fully derivable from the source artifact with no author input. Carries two `doctor` checks — the grain claim moves **post-filter**, and a new **`filters narrow`** check counts kept-vs-total rows over the probe window, failing on `kept == 0` (dialect-hostile predicate) and warning on `kept == rows` (constant-true — C15's defect through a new door). The invariant that makes v1 safe is **total resolution or skip**: every predicate resolves or the metric stays refused, so v1 is a strict superset of today's behaviour whose only failure mode is refusal. **This is v1 of three** — cross-relation references and per-input filters on ratio/derived are separate items (spec §13). **Shipped:** `BindingSpec.where` (a list of ANDed, unqualified predicates over the binding's own relation) plus a resolver in `dbt_bridge` that substitutes each `{{ Dimension('<ref>') }}` for a placeholder, parses the result **in the target dialect**, and then requires that *every* `exp.Column` in the parse tree be one of those placeholders — which is what makes total resolution literal rather than aspirational. That last check refuses a construct the spec did not anticipate and the roadmap row would have let through: a **legacy raw-SQL predicate** (`amount > 0`, one of the four serialisations C15 taught us to normalise) has no `Dimension()` call to resolve, and the name it is written with need not be the column MetricFlow resolves it to — a dimension `region` may read `customer_region` — so qualifying it against the fact relation either errors or silently means something else. It is refused, which keeps the only failure mode refusal. In `dbt_sql`, `_bounded` gained the predicate alongside the window bounds and **all five builders now route through it**, so *every diagnostic sees the same rows as the series* is structural rather than remembered. `doctor` gained `filters narrow` and a fourth `CheckResult` status: `kept == rows` **warns** rather than failing (the spec's own open question — a vacuous filter over a seven-day probe window is real and must not block a correct tree) and does not affect the exit code. Proven end to end on DuckDB: an unfiltered metric of 750.0 over 6 rows against 500.0 over the 4 rows its `is_food_order` filter keeps, with the filtered slices summing to the filtered total. **Deliberately not shipped from the spec's §12 step 5:** `where` on the `/metrics` payload. The predicate is already visible where it matters — *show query* renders the generated SQL beside the number — and putting provider-internal binding state on a node payload is a new coupling for a disclosure that exists. The `doctor` bind-step count did ship, since that is where a reader looks at startup. | ✅ | **C15 made this urgent by making it visible.** Filtered metrics used to "work" — wrongly, serving the unfiltered relation — and now they are refused by name, which is correct and is also a capability the tree author can see they don't have. The review put filters at **~80% of real trees**, and `filter` is the single most common way a dbt metric narrows a measure, so this is the difference between the dbt path serving a real project and serving a demo. It is a follow-on rather than part of C15 because C15's job was to stop serving a wrong number, and that had to ship without waiting for a binding-language change |
| 2.19 | **`fill_nulls_with` is refused, and it is most of a real project** — [C15](#horizon-0--correctness-numbers-the-engine-cant-defend) refused four semantics-changing MetricFlow fields together, correctly: a measure declaring `fill_nulls_with: 0` means something different from the measure without it, and `BindingSpec` cannot express the difference, so serving it would be a silent wrong number under the governed metric's name. [2.17](#horizon-2--make-it-repeatable-a-stranger-can-onboard) restored `filter`, the one the review had costed at ~80% of metrics. **The cost of the other three was never costed, and it is larger than the one that was.** Measured 2026-08-13 by running `translate()` against the White Cube dbt project's real semantic manifest — the only complete dbt project in reach: **0 bindings, 5 formulas, 22 skipped**, of which **18 are `fill_nulls_with`**. The `dbt` provider cannot serve that project *at all*, on any warehouse. Three of the remaining four skips are the rate metrics, refused for a different and cheaper reason: their expressions are `num / nullif(den, 0)`, which is the canonical safe idiom, and `_SIMPLE_RATIO` recognises only `num / den` — so the *most correct* way to write a ratio in dbt is the one shape the bridge does not accept. Scope, cheapest first: teach the ratio matcher `nullif`; then decide what `fill_nulls_with` needs, which is either a `fill:` on `BindingSpec` compiled into the generated SQL (the 2.17 shape, and note `fill_nulls_with: 0` interacts with [1.11](#horizon-1--prove-it-a-trustworthy-reproducible-rca) — a filled zero and a genuinely undefined period must not become the same thing) or a documented refusal we are content to keep. `join_to_timespine` is the third and is likely the same fix. **The cheapest part shipped as part of [1.12](#horizon-1--prove-it-a-trustworthy-reproducible-rca):** `_translate_derived` now rewrites the exact `num / nullif(den, 0)` idiom to `num / den` before translation, narrowly — a different guard value or function is still refused by name. **A second, narrow slice of `fill_nulls_with` shipped 2026-08-15:** `_translate_simple` now accepts `fill_nulls_with: 0` on a `simple` metric whose resolved aggregation is flow-shaped (everything `AGG_MAP` supports except `average`) — proven safe rather than assumed, because `data_fetch.py` already zero-fills every missing period of a `kind: flow` node, so accepting or refusing this exact case produces the identical series; refusing it bought nothing. **One correction to that proof (2026-08-17):** "identical" holds at interior and leading gaps but **not at the trailing edge** — `_align_to_spine` *trims* trailing gaps before its flow fill runs, where MetricFlow's timespine would report zeros, so a lagging mart diverges at exactly the tail and `/meta.data_through` will differ from the governed metric's claimed coverage. The acceptance stands (breakdown's trim is the better answer, by C2's own reasoning); the stated safety argument was wrong at one edge, and the docstring at `dbt_bridge.py:623-629` needs the same amendment. A non-zero fill, a fill on an `average` (`kind: rate`) metric — the 1.11 landmine this row already flagged — a `ratio`/`derived` metric of any kind, and bare `join_to_timespine` with no paired `fill_nulls_with` are all still refused exactly as before, unconditionally. **What this actually closes on the one real project in reach is still unmeasured** — the White Cube dbt project is external and not available from this environment, so whether most of the 18 `fill_nulls_with` skips are flow-shaped-and-zero (closed by this) or `average`/non-zero (still refused) is unknown; re-run `translate()` against it before revising the "18 of 22" estimate. `join_to_timespine` (bare) remains a fully open third construct, and a general `fill:` mechanism for non-zero/rate-shaped fills was deliberately not built — narrow acceptance of a provable coincidence, not a new capability, per the provider-boundary rule | ○ | 2.17's row argues that filters are "the difference between the dbt path serving a real project and serving a demo". That argument was right and incomplete: on the one real project available, filters were **not** the binding constraint — `fill_nulls_with` is, by 18 metrics to 0. This is worth stating plainly because it is the second time this boundary has been sized by estimate rather than measurement, and the second time the estimate was wrong in a way that flattered the provider. Run the bridge against a real manifest before costing the next one |
| 2.18 | **Request-lifecycle and deployment hardening** — ✅ **shipped 2026-08-12.** The non-wrong-number half of the second review's findings, taken as one pass because they are one shape: an operation that is unbounded, or that runs somewhere it should not. **Bounded:** exact Shapley refuses above `_MAX_SHAPLEY_PARENTS = 10` parents (the enumeration is O(2ⁿ) and RCA runs it six times per formula node while holding the tree's lock — measured 10 parents ≈ 3.5s, 12 ≈ 20s, 14 ≈ 80s; the remedy is intermediate sums, which preserve the identity, and the cap matches `simulate.py`'s `_MAX_SOURCES` for the identical loop); `POST /rca/{name}/slices` refuses a window outside the loaded data *before* fetching, where a `1900-01-01 … 2100-12-31` request previously returned 422 **and** left a permanent cache entry; `slice_cache` and `flow_cache` are bounded at all (C8 bounded `traces` and never looked at its two siblings); and the trace cap became **bytes-first** — `MAX_CACHED_TRACE_BYTES = 512 MiB`, overridable — because an entry scales with the loaded window, so 256 entries measured 13.4 MB each on the demo's own 830-day window, ~3.4 GB against a 2 GB box, and no entry *count* can be made safe. **Moved off the event loop:** `GET /metrics/{name}` summarized a trace synchronously in an `async def` — 1.1s of total outage per call on an 830-day fit, scaling with `draws`, on every call because nothing memoized it; now `to_thread` plus a memo on the `FitResult` (1.19s → 0.02s on repeat). **Ordered correctly:** `run_rca` validates window coverage *before* fitting, instead of paying every ancestor's ADVI fit and then 422ing. **Degraded instead of failing whole-tree:** a node whose fit raises (a parent held identically at zero — a seasonal business's default state, and the exact shape [C4](#horizon-0--correctness-numbers-the-engine-cant-defend) was confirmed on) now reports `status: "fit_failed"` and every other node still reports. **Auth:** `BREAKDOWN_API_TOKEN` gated `/mcp` alone while `/series`, `/meta` and `/dag` answered anyone — and `/dag` serialized `sql` and `bind`, publishing fully-qualified table names and WHERE-clause logic. Now `/dag` redacts both for an unauthenticated caller whenever a token is configured, and opt-in `BREAKDOWN_REQUIRE_AUTH` extends the same bearer check to every route but `/`, `/health` and `/ui` (matched on segment boundaries), refusing to boot if set without a token rather than failing open. A non-ASCII `Authorization` header is a 401 rather than a `TypeError` 500. **UI:** the two new statuses plus `window_shorter_than_grain` render as *not analyzed* in one amber vocabulary across canvas, ranked causes, attribution detail and the exported report, and the summary strip says how many nodes in scope could not be analyzed — which matters more than it sounds, because `_rank_causes` emits every in-scope node, so a failed node can **top the ranking** while everything upstream of it silently scores 0. Fixing that surfaced a latent defect the review had not found: `applyRcaOverlay` tinted on `node.gap >= 0`, and **`null >= 0` is `true` in JavaScript**, so a node with no measured gap had always rendered green with an upward arrow — the honesty posture inverted, in the one surface a client actually looks at. Also fixed: `ci_status: "posterior_only_single_period"` had never been rendered at all, so a withheld interval read as a clean one. **MCP:** `compact_rca` shrank every non-`ok` node to `{status, grain}`, dropping the reason and the gap an `attribution_failed` node still measures — the [C9](#horizon-0--correctness-numbers-the-engine-cant-defend) failure mode through a new door, an agent-facing payload that invites the wrong narration — so both are kept now and `how_to_read` says a non-`ok` node is a gap in the analysis rather than a quiet metric | ✅ | Two viewers share one process, one lock and one cache, so every unbounded operation here is one careless request away from being everyone's outage — and the second review reached 261 cached traces, a 73,000-day provider scan and a 25-second freeze through the **public API**, without malice. The auth item is the one that changes what a deployment *is*: the operator believes the box is gated because `/mcp` returns 401, and the README did not mention the variable at all. Full gating stays opt-in because gating the data routes also gates the browser UI's own fetches, which is a hosted-mode question ([3.5](#horizon-3--make-it-findable-and-sticky-it-comes-to-you)), not a defect fix |
| 2.20 | **Snapshot-mode operational honesty** — the two 2026-08-12 review findings that survived every fixing pass, promoted from the archived triage because they block the first client deployment. *(a)* **`SnapshotStore.read` requires an exact window match** (`snapshots.py:139-156`) while `read_sliced`, in the same file, documents *why* serving a stored window that **contains** the request, trimmed, is correct — and does it. Same file, opposite policy, undiscussed: nudging `--end-date` by one day takes a snapshot-served deployment to `degraded` across every metric, for a config change that *shrank* the request, and a first deployment is precisely iterative window-fiddling against a live snapshot dir. `_sliced_candidates` is ~90% of the code needed. *(b)* **`doctor` cannot see snapshots** — `doctor.py` builds its fetcher with `_build_fetcher` directly, never `_wrap_snapshots`; the word "snapshot" appears nowhere in the file. On the snapshot-only deployment mode the README and demo both ship, `doctor` prints `[FAIL]` against a server answering perfectly, contradicting its own docstring ("proves the same code path the server will use, not a lookalike") — and since [1.12](#horizon-1--prove-it-a-trustworthy-reproducible-rca) made the exit code meaningful, it is now meaningfully wrong. The two interact: the operator sees `degraded` from (a), runs `doctor`, and gets a false FAIL from (b). **Shipped 2026-08-17, together.** *(a)* `read` shares one `_containing` scan with `read_sliced` (exact match first, then narrowest span), with one guard the sliced path also gained: a stored window whose *bounds* contain the request but whose *data* does not is a miss, not a hit — serving the empty trim would hand `_align_to_spine` nothing and a `flow` would come back as a full window of invented zeros. *(b)* snapshot-dir resolution moved into `snapshots.py` so `doctor` and `_wrap_snapshots` cannot disagree about where snapshots live; a `snapshots` check inventories per-metric coverage; **full coverage downgrades provider-chain failures to warnings that say why**, so the exit code tells the truth on a snapshot-served box; fit readiness fetches through the same snapshot-wrapped path the server uses (its "never a lookalike" docstring was false without this); and the `BREAKDOWN_START_DATE`/`END_DATE` pair the server reads counts as an explicit window, so an env-configured deployment is checked over the window it will actually load | ✅ | Neither is a wrong number; both are the tool declaring itself broken when it is not, which at a first deployment costs trust at the same rate a wrong number does — and every failure path in the product points the operator at `doctor`. Fix both together, before the deployment: ~1 day combined |
| 2.21 | **A localization verdict may not be headlined by the roll-up bucket** — `POST /rca/{name}/slices` publishes `localized` (the verdict the UI and MCP read verbatim since [C24](#horizon-0--correctness-numbers-the-engine-cant-defend)), and the rule behind it is excess-over-gap >= 25% on the top slice. Nothing restricts *which* slice may headline it: on story B's windows, `customer_churn_rate` by country returns `localized: true` because **`__other__`** — the top-k roll-up, not a segment — carries 26.4% excess, a hair over the bar, while the best *named* slice (US) carries 5.2% and five of nine rows are noise-flagged. Measured 2026-08-19 during the engagement-edges rework (breakdown#81); the guided tour moved its "a tier, not a geography" contrast to `churned_mrr` to route around it. The fix is a vocabulary, not a threshold tweak. "Localized in `__other__`" reads as a finding, but the reader's next move — act on the segment — does not exist, because `__other__` is the set of slices nobody enumerated; suppressing it to "not localized" is wrong the other way, because the tail genuinely did move. When the roll-up tops the ranking, say what is actually known: *concentrated in the long tail — raise `top_k` (or slice a different dimension) to see inside*, as its own verdict state. Decide it in `slices.py` where the verdict is published, so the UI, MCP and the test suite inherit one rule instead of re-deriving three. **Shipped 2026-08-22**, as a vocabulary: `_localization` publishes `localization` — `localized` / `long_tail` / `not_localized` — beside the numbers it summarizes, and the second gate is *the leader must be a segment*, so `__other__` clearing the threshold reclassifies rather than headlines. A verdict whose next move is a *setting* rather than a segment has to name the setting, so the same function publishes `localization_remedy` — one sentence, rendered by the panel and read by an agent, deliberately not a caveat (the panel already prints the verdict, and the duplicate paragraph on screen — seen by opening it — read as two findings). `localized` stays exactly the narrow "may I print *X carries N% of the gap*?" boolean and is therefore `false` under `long_tail`: a consumer written before this row is restrained rather than misled, which is the whole backward-compatibility story. The UI renders the third verdict in warn tone with `top_k` named, and — a fifth-rule fix found by opening it — the leading row is now tinted **only** under `localized`, because a tinted `__other__` row under a long-tail verdict says the opposite of the sentence above it. `SLICE_HOW_TO_READ` gains the three states and the instruction never to name `__other__` as the culprit; `compact_slice` carries all three fields. Opening the UI also caught a plain JS defect the suite could not: the new `const state` inside `sliceResultHtml` shadowed the global `state` the same function reads on its first line, so **every** RCA rendered "RCA failed: Cannot access 'state' before initialization" — no test runner, no red, exactly the gap the fifth rule names. The measured case is pinned (`test_story_b_the_churn_rate_by_country_is_a_long_tail_verdict`), and with `top_k` raised to 20 on that same window the verdict falls to `not_localized` (best named slice AU, 13.8%) — the check that `long_tail` was the honest reading and not a softened `localized`. The tour keeps its contrast on `churned_mrr`: two opposite verdicts on one node is a cleaner beat than three states, and the churn-rate panel is now scripted as the optional third-verdict demo | ✅ | "Not localized" restraint is the pitch — the demo scripts it as the reason the localized verdicts are worth believing — and a verdict that names the bucket of unenumerated leftovers as the culprit spends exactly that credibility. The marginality is its own small lesson: 26.4% against a 25% bar is a verdict that flips on draw noise, and the roll-up case is where a marginal verdict is also a meaningless one |

*(2.12 — a `bsl` binding — was removed 2026-08-11. It existed to serve a local
parquet file with no warehouse, and [2.10](#horizon-2--make-it-repeatable-a-stranger-can-onboard)
made that possible without it: the binding contract plus the DuckDB connector
run `relation: read_parquet('…')` or a `bind.sql` over the file, verified end to
end. The residue — a provider type that reaches per-node bindings without a dbt
project on disk — is a fraction of a BSL adapter and folds into 2.2. What was
left of the case was "someone might already author BSL YAML", which principle 1
rules out and which this roadmap already applies to Cube, Rill, Lightdash and
Superset. IDs are not reused.)*

*(2.7 — hybrid cold-start→fitted mode — was removed 2026-08-05; see
[Deliberately not on the roadmap](#deliberately-not-on-the-roadmap). IDs are not
reused.)*

**Exit:** a new tree onboards in < 1 day; an external tool runs an RCA against a demo tree via MCP; a company can keep a tree per quarterly goal and see them all in one place.

---

## Horizon 3 — Make it findable and sticky: it comes to you

Gate: real, recurring usage asking for these — **except 3.1**, whose gate was
removed (2026-08-05) as circular. "Wait for recurring usage" cannot gate the item
whose entire purpose is to *create* recurring usage. A tree exercised only during
incidents is an insurance artifact, and insurance artifacts rot: nobody owns the
tree, no test fails when a dbt model is renamed or re-grained, and the decay is
invisible until the week someone needs it. Declarative semantic assets survive
when they sit on the critical path of something a person reads on a schedule.
3.1 is what puts the tree there.

| # | Item | Status | Why |
|---|------|--------|-----|
| 3.1 | **Scheduled evaluation + anomaly flagging + digest** — "revenue moved; order_count explains ~80%, CI attached — link to full RCA" | ○ | Converts breakdown from pull to push; the retention feature and the seed of the ops layer. **Ungated** (see above): a tree that produces the Monday business review gets fixed when it breaks, and nothing else on this roadmap creates that obligation |
| 3.2 | **Dimensional slicing inside the tree** — attribute a node's gap across a declared dimension (geo, plan, channel), warehouse-side | ◑ | Completes the traverse + slice workflow. Flat slicing is commoditized; *tree × slice* is not — the biggest pure-product differentiator. v1 shipped per [`dimensional_slicing_design.md`](dimensional_slicing_design.md): `dimensions:` schema, exact sum/Bennet slice attribution with excess ranking, mock + SL providers, `POST /rca/{name}/slices`, MCP `slice_metric`. **UI** and **sliced snapshots** landed with the White Cube demo (3.7): a slice panel per ranked cause, lag-correct windows, and a concentration threshold so the panel says "not localized" rather than naming whatever sorted first. Remaining: automated tree×slice. **The warehouse SQL contract and its doctor checks moved to [2.8](#horizon-2--make-it-repeatable-a-stranger-can-onboard)** — they gate onboarding, not stickiness |
| 3.3 | **Native metric-view connectors** (e.g. Databricks metric views) — **largely superseded (2026-08-10) by [2.9](#horizon-2--make-it-repeatable-a-stranger-can-onboard)**: generating SQL through sqlglot dialects covers Databricks/Snowflake/BigQuery/Postgres/DuckDB in one provider, and 2.9's whole point is that it needs no dbt SL. What survives here is genuinely *native* metric objects (Databricks metric views, Snowflake semantic views) whose definitions live outside dbt entirely | ○ | Hedge against thin dbt-SL adoption; slots in as another `BaseDataFetcher` + scaffolder. The hedge is mostly paid for once 2.9 ships — the residual is shops with no dbt at all |
| 3.4 | **Counterfactual RCA** (T11: posterior-predictive forecast) — "the drop was X units below what the normal regime predicts (95% CI …)" | ○ | Upgrades the flat-trend approximation; strong headline number. Distinct from the existing steady-state what-if |
| 3.5 | **Hosted mode** — auth, scheduled refresh, fit queue + warm cache | ○ | The operational product layer; PyMC fits are CPU-heavy, so a queue + cache is required |
| 3.6 | **Domain template packs** — worked example trees + methodology for specific domains (e.g. emissions/impact driver-tree decomposition) | ○ | Content that doubles as onboarding examples and demonstrates breadth |
| 3.7 | **Deployable demo instance** — a hosted Breakdown over synthetic B2C SaaS data ("White Cube") with planted, ground-truth-labeled anomalies, per [`white_cube_demo_plan.md`](white_cube_demo_plan.md). **Amended (2026-08-19): the engagement-edges rework shipped** (breakdown#81 / fake_companies#3) — `new_subscriptions` became a measured cohort-aligned lagged identity over the three doors into a paid plan, and the tree carries three genuinely learnable edges (trial activation and days-active -> conversion, member activity -> churn), planted in the generator through shared engagement drivers and weekly churn sub-ticks; every tour figure re-pinned. **Closed (2026-08-22): the last open fidelity item, `dau_over_active` not meaning what it says, is fixed** (fake_companies `7a0e9b2` -> breakdown#88). The config documents the knob as *per-plan P(an active-sub user is active on a day)* (0.14 studio / 0.24 professional); `usage.py` had been multiplying it into an NHPP event *intensity* — lambda = dau x events_per_active_day, roughly 1.7–5.3 events/day — so the realized active share, P(>=1 event), ran 0.77–0.93, and the repo held two readings of one knob (`trial_activity.py`, written later, already treated it as the probability it claims to be). `usage.py` now draws each user-day the same way, Bernoulli on p_active then counts conditional on active, through shared helpers so there is one reading. Measured on the demo canvas: weekly `member_activity_rate` **0.927 -> 0.251 mean** (max 0.981 -> 0.316), off the ceiling and into a working range; weekly corr(activity, the shared `member_engagement` driver) **+0.28 -> +0.74**, so the activity->churn edge now works *because* of the construction rather than despite it; DAU/WAU 0.359 -> 0.216. The re-pin was bounded exactly as predicted — **4 of 59 committed snapshots** changed, all 11 planted stories still verify, and stories A, C, the trial-volume controls and story B's plan slice are unmoved. Story D's two engagement metrics moved (activation +34.4% -> +35.9%, days-active +71.2% -> +76.1%) because both are recomputed in dbt from `product.events`; conversion's +40.2% did not, because it comes off the lifecycle | ✅ | The pitch artifact: a link a prospect can actually use, live and auto-deployed on every push to `main`. Pulls three engine items forward as a side effect — the slicing **UI** and **sliced snapshots** (both 3.2 "Remaining"), and a bearer-token gate on `/mcp` (a down payment on 3.5). The two `⚠ known gap` notes still in the guided tour (`churn_arpu` renders uncoloured with no declared `direction`; the RCA table renders no `lag`) are product defects filed against the UI, not unfinished demo work |
| 3.8 | **Non-additive metrics at entity grain** — a `count_distinct` metric's slices overstate the total by their deduplication overlap, and the engine currently reports that as `reconciliation.status = "discrepant"` in red, which reads as a broken pipeline rather than as arithmetic. Three tiers by what the author declared: with `entity_key` **and** an entity-grain relation, a declared `resolve` rule (`first`/`last`/`error`, never a default) makes the assignment single-valued and the metric **exactly** additive, so the existing sum attribution runs unchanged; with `entity_key` alone, the overlap is named and quantified and contribution *shares* are withheld; with neither, trend only. Adds the **entity-flow** diagnostic — new / churned / retained / migrated, with migration netting to zero across slices exactly as a rate's `mix_total` does. Per [`non_additive_slicing_design.md`](non_additive_slicing_design.md) **Shipped in three parts.** §5 labelling: `additivity` comes from the binding, never the residual, and overlap is named rather than flagged `discrepant` — the flag now means only *unexplained* divergence. §4 resolution: `entity_grain` with a declared `resolve` (no default; `first` and `last` gave `{active: 2}` vs `{active: 1, cancelled: 1}` on the same day) makes the slices sum exactly. §6 flows: new/churned/retained/**migrated**, migration netting to zero. Verified on a real warehouse — naive 4,559 vs metric 4,484, resolved exactly 4,484, and 36 subscriptions cancelled → active. Flows are a **diagnostic beside** the attribution, never a second decomposition: they compare window-level sets, which do not reconcile to a window-mean gap (`reconciles_to_gap: false`). **Two follow-on defects found by the 2026-08-17 audit**, neither a wrong number, both propagation: *(i)* `resolve: error` is honoured in the slice query (`build_resolved_slice_query` short-circuits, saying why) and **silently executed as `last`** in the flow query — `build_entity_flow_query`'s `order = "ASC" if resolve == "first" else "DESC"` lets `error` fall through, untested in either direction; **fixed 2026-08-17** — the flow query now refuses under `error` with the remedy named (the per-period assertion says nothing about the per-window representative row this query needs), and doctor's entity-grain check separates *could-not-check* from *assertion-violated*, which it had been conflating. *(ii)* MCP's `compact_slice` drops `additivity`, `overlap` and `entity_flows` entirely, and `SLICE_HOW_TO_READ` never mentions `not_applicable` — so the consumer most likely to narrate a platform switch as two offsetting causes was the one surface that never saw the migration diagnostic built to prevent exactly that — **fixed 2026-08-17 with [C24](#horizon-0--correctness-numbers-the-engine-cant-defend)**: `compact_slice` carries `additivity`, `overlap`, `entity_flows` and the `localized` verdict, with `how_to_read` clauses for each | ✅ | Measured on a real warehouse: `active_subscription_count` sliced by status came to **2,106 against an unsliced 2,069** — 37 subscriptions that changed status inside a day, counted once in the total and once per status. The labelling is the sharper defect: [3.2](#horizon-3--make-it-findable-and-sticky-it-comes-to-you)'s reconciliation lumps a mathematical property together with a diverging query and freshness skew, so a user is told their pipeline may be broken when nothing is, *and* the flag stops being trustworthy for the cases that are real. The flows are what produce findings: a user switching platform shows −1 on iOS and +1 on web with the total unchanged, and naive slicing reports two large offsetting causes for a change that never happened |
| 3.9 | **A documentation site** — render `docs/` as a navigable site (MkDocs Material is the default choice for a Python project of this shape; the files are already markdown in the repo, so this is a generator, a CI job and hosting rather than a rewrite). Stage 3 of the split begun in [2.6](#horizon-2--make-it-repeatable-a-stranger-can-onboard), and **filed here rather than there because its gate is different.** Stages 1 and 2 are content moves whose payoff is bounded and obvious; a site is infrastructure, and this horizon's gate — *real, recurring usage asking for it* — is the honest bar. The signal to watch for is people getting lost, or asking questions the README cannot answer at whatever size it has settled at; not that the docs would look nicer rendered | ○ | Once `docs/` holds a reference, a tutorial and an operations guide, flat markdown on GitHub stops being navigable — no search, no sidebar, no versioning against a package whose YAML schema is its public API. But none of that is true *yet*, and building it early would spend a CI job and a hosting decision on an aesthetic. Related, and deliberately not merged into this row: the README stays the PyPI landing page whatever happens here, so it has to stand alone regardless |

---

## Statistical rigor (S) — a standing workstream

Not a horizon: a parallel, priority-ordered track that runs alongside them. The
items come from §4 of the [statistics white paper](statistics_whitepaper.md),
which holds the *statistical rationale* for each — why the gap matters, what the
literature says, what "fixed" would mean. **This table is the source of truth
for status and sequencing**; the white paper cites these IDs and does not
duplicate the schedule.

**Sequencing decision (2026-08-05):** this track runs **immediately after the
0.1.0 release**, ahead of the adoption items (2.2, 2.6). The reasoning is that
the cheapest moment to change what a credible interval *means* is while the user
base is still small — once people have RCAs in circulation, widening intervals
means telling them their past analyses were overconfident. It does not block the
release itself: 0.1.0's limitations are documented rather than hidden, which is
the bar principle 3 sets.

**Amended (2026-08-05, same day):**
[**Horizon 0**](#horizon-0--correctness-numbers-the-engine-cant-defend) runs
**ahead of this track**. The distinction is the one a reader most needs: an S
item is a *known limitation that is disclosed*, which principle 3 permits; a C
item is behavior the docs describe wrongly or a number the engine cannot defend
at all, which it does not. Fix the second class before improving the first. Two
C items have S counterparts and the split is deliberate — C4 fixes the block-cap
*bug*, S6 estimates the block *length*; C7 bounds cold-start draws, S7 correlates
the beliefs behind them.

| # | Item | Status | Why |
|---|------|--------|-----|
| S1 | **Benchmark full-rank ADVI** as the RCA default — `pm.fit(method="fullrank_advi")` fits a full covariance matrix instead of a diagonal one. Measure fit time and interval width against both mean-field ADVI and NUTS on the calibration suite and the White Cube tree; adopt if the cost is acceptable — **benchmarked 2026-08-18, and the answer is no.** Full measurement in [`s1_fullrank_advi_benchmark.md`](s1_fullrank_advi_benchmark.md), reproducible via [`benchmarks/s1_fullrank_advi.py`](benchmarks/s1_fullrank_advi.py); `fit_metric` keeps `inference_method="fullrank_advi"` and the new `vi_iterations` knob as the benchmark's substrate, engine-level only | ✅ | The synthetic promise is real: at convergence (40k optimizer steps, not the engine's 20k default — at 20k every full-rank fit is `suspect` with width ~1.8× NUTS and falling) full-rank reproduces the NUTS interval on the β-vs-trend ridge to within 4% where mean-field is ~20% narrow, exactly as [`advi_vs_nuts_in_breakdown.md`](advi_vs_nuts_in_breakdown.md) predicted. **On the real tree it fails the adoption test on both axes at once**: the two seasonal White Cube nodes took ~230s each — 3.5–20× slower than NUTS *on the same node*, ~100× mean-field — and on `sessions` the converged, `ok`-ELBO full-rank interval is **7.8× the NUTS width** (over-dispersed this time; ~120 latents is where the O(d²) covariance both slows the optimizer and mis-fits). At the bundled demo's window size — 830 daily periods, ~830 trend latents — the scaling completes the verdict: **254s per fit, 23× slower than NUTS, 12× the NUTS width, ELBO clean**, while NUTS itself costs 11s there; and mean-field at its shipped 20k steps is unconverged on every 830-period fit and lands 2.1× *over*-wide, so VI error on this model is unpredictable even in direction (0.8× on the short ridge, 2.1× long, 7.8× over for full-rank on a real node). So the measurement did the opposite of retiring S2 — it produced a *second* VI variant with a measured ELBO-passes-but-wrong mode — and it repriced NUTS: 3.4s on the 130-period calibration worlds, 11–66s on real weekly nodes, cheap enough that per-node escalation (S2) is clearly affordable. Two side findings for the record: mean-field at its shipped 20k steps is `suspect` on all three real White Cube probabilistic nodes, and the `new_subscriptions` NUTS fit collapses to a ~zero-width `beta_raw` (flagged `suspect`) — investigated and explained: in the committed snapshots that edge is *exactly* deterministic (`corr(y, x.shift(1)) = 1.0`), so the posterior is a point mass (β=1, σ_obs→0), which NUTS grinds against (R̂ 4.4, ESS 4) and honestly flags, while both VI variants report a *non-zero* width on an edge with zero uncertainty. The tree's own comment promises resurrections should put the coefficient "just above 1"; the generated data delivers none on this edge, which is a demo-data fidelity note, not an engine defect. **Fixed 2026-08-19 by the White Cube engagement-edges rework**: `new_subscriptions` is now a measured cohort-aligned lagged identity over trial conversions + reactivations + direct conversions, and the demo gained three genuinely learnable probabilistic edges (trial activation and days-active → conversion, member activity → churn) planted in the generator — see [`white_cube_demo_plan.md`](white_cube_demo_plan.md) |
| S4 | **Parent collinearity diagnostic** — pairwise correlation (or VIF) among a node's parent regressors over the fit window; warn when the split of credit is unstable | ○ | **Promoted (2026-08-05)** from below S3 to here. It sits on the shortest path to a wrong decision: correlated parents produce a well-determined *sum* and an unstable *split*, and the split is exactly what RCA reports — while mean-field ADVI reports a *narrow* interval around whatever arbitrary split it landed on. Nothing warns, and no test covers it (see S17). The reference tree contains the structure itself: `true_trial_conversion_rate` regresses on parents including `trial_to_pql_rate` while `product_qualified_leads` is *defined* as `true_trials × trial_to_pql_rate`. Cheap, self-contained, composes with the existing `sign_warnings` channel. **Correction (2026-08-08):** this row used to cite the reference tree as containing the structure itself — `true_trial_conversion_rate` regressing on `trial_to_pql_rate` while `product_qualified_leads` was defined as `true_trials × trial_to_pql_rate`. [C10](#horizon-0--correctness-numbers-the-engine-cant-defend) removed it, because a reference tree people copy is the wrong place to keep a known-bad structure. S4 and S17 now need a purpose-built collinear fixture instead of a live example |
| S2 | **A real ADVI approximation diagnostic** — PSIS-based k̂ (Yao et al., 2018) reported per fit; where k̂ is poor, auto-escalate that node to NUTS or mark its intervals unreliable in the response | ○ | Today's `fit_quality` for ADVI checks only that the ELBO stopped moving, so a well-converged *bad* approximation passes as `"ok"`. This detects the failure instead of assuming it away, without paying NUTS cost on every node. **S1's result (2026-08-18) set the scope and raised the priority — now next up.** Full-rank did not make this unnecessary; it added a second VI variant with a measured `ok`-ELBO-but-wrong mode (7.8× over-dispersed on a real node), so a real approximation diagnostic is the only honest gate on any VI interval. And S1 repriced the escalation: NUTS runs 11–66s on real weekly nodes, so k̂-triggered escalate-to-NUTS is affordable in exactly the interactive setting that made NUTS-everywhere unusable |
| S3 | **Posterior predictive checks on every fit** — simulate replicated series from the posterior, compare summary statistics against the observed series, flag nodes whose data sits in the tail; surface through the existing `fit_quality` channel | ○ | The single most informative Bayesian model check there is (Gelman et al., 2020) and the one the engine most conspicuously lacks. Needs no new modeling. A badly misspecified node currently passes silently as long as it converges |
| S5 | **Simulation-based calibration** (Talts et al., 2018) — draw parameters from the prior, simulate, refit, check that the rank of the true parameter within the posterior is uniform | ○ | The definitive test that inference is calibrated. Turns 1.2's single-scenario coverage test into a real guarantee. Expensive: a release-gate or nightly job, not per-commit |
| S6 | **Data-driven bootstrap block length** (Politis & White, 2004), replacing the fixed per-grain constants in `BOOT_BLOCK`. **Promoted and re-scoped 2026-08-13 by [C4](#horizon-0--correctness-numbers-the-engine-cant-defend)'s measurements**, which were taken to justify the block *cap* and incidentally measured the block *value*. Two findings this item now owns. **(a) `BOOT_BLOCK["day"] = 7` resonates with a weekly cycle.** A 7-day block contains each weekday exactly once, so a weekly seasonal component cancels *identically* in every replicate. Sweeping the block on the bundled demo tree, the published `average_order_value` interval width runs b=1 → 768, b=2 → 1037, b=3 → 1028, b=5 → 747, **b=7 → 299**, b=13 → 285: the shipped default is a **local minimum** of the honest width, not a plateau. It repeats at 28-day windows (b=7 → 503 vs b=3 → 860). No cap can fix it — the cap chooses *how far down* from the constant, and the constant is the problem — so every daily-grain interval on a weekday-seasonal metric has been optimistic by roughly a factor of three, and is disclosed as such in [`docs/model.md`](../docs/model.md) until this lands. **(b) Under serial dependence, short windows are weakly informative whatever the block** — 0.17–0.61 of the true sampling variance across every block length tried on an AR(1) at ρ=0.6. That part is inherent to the window rather than to the estimator, so it is a disclosure, not a defect, and it is why C4 closing does **not** fully retire the "treat a short-window CI as a lower bound" advice | ○ | Block length currently is not estimated from the data at all. Too short understates uncertainty, too long overstates it — and the current values were reasonable guesses, which is now measured rather than suspected: the daily constant is the single worst choice in its neighbourhood on exactly the seasonal daily data the engine is most often pointed at |
| S7 | **Correlated cold-start beliefs** — let authors declare correlations between priors (or a joint distribution over a small set of beliefs) | ○ | The largest modeling gap in cold start: beliefs are sampled independently today, so "if price lands high, conversion lands low" is unrepresentable and intervals are wrong in either direction wherever beliefs genuinely co-vary. Already disclosed in every cold-start response |
| S8 | **Local linear trend as an opt-in** — a trend with a slope component, chosen per node in the YAML; local level stays the default | ○ | A node with genuine momentum is currently modeled as a level that happened to move, which pushes momentum onto the parents. Keep the tight-prior default (it does deliberate work) and give the exception an escape hatch |
| S9 | **Narrow nonlinear edges** — a declared transform on a specific edge (`response: log` on ad spend → conversions), not a modeling language | ○ | Covers the most common nonlinearity (diminishing returns) without opening the door to arbitrary model complexity. MVP-first: one named transform |
| S10 | **Posterior predictive plot in the UI** — observed vs replicated series per node | ○ | The most persuasive single visual a Bayesian tool can offer, and nearly free once S3 computes it. *Blocked on S3* |
| S11 | **Prior-vs-posterior visualization** per coefficient — "you believed 0.1 ± 0.02; the data says 0.08 ± 0.01" | ○ | Makes the Bayesian update concrete and teaches the model while it informs. Directly serves the reader/reviewer persona 1.4 targets |
| S12 | **Make `ranked_causes` visibly a heuristic in the UI** — distinguish "ranked by triage score" from "ranked by evidence", or attach the underlying interval so a wide-interval cause cannot outrank a tight one on a point estimate | ○ | It is documented as triage and rendered as the most prominent number in the UI. Prominence implies rigor whatever the docs say |
| S13 | **Methods appendix in the exported report** — a linkable expansion of the existing methods footnote stating fit window, inference method, diagnostics, and the caveats that applied to *that* analysis | ○ | Makes an exported RCA self-defending when it circulates without its author — the whole point of 1.5's export |
| S14 | **Quantify the DAG assumption** — a sensitivity statement: "if an unmodeled confounder explained X% of this parent's movement, the attribution would change by Y" | ○ | Puts a number on the assumption everything rests on. Highest ceiling and least defined item here; adapting unmeasured-confounding sensitivity analysis to metric trees is research-flavored. Note this is *not* causal discovery, which stays off the roadmap |
| S15 | **Multiplicity and selection-aware reporting** — disclose first (`how_to_read`, [`docs/model.md`](../docs/model.md), the UI), then evaluate whether the reported interval on the *selected* top cause can be made selection-aware | ○ | A single `run_rca` on a 15-node tree emits 25–30 intervals plus a `prob_same_direction` each, sorts by effect size, and presents the top one — whose `ci_95` was computed **pre-selection**. Under a global null ~1.5 of those intervals exclude zero by construction, and the window pair is a free user choice with no cost to retrying. The README's own essay makes this argument against unconstrained agent search; we have a bounded version of the same problem and disclose it nowhere. *Considered and not chosen as the fix: hierarchical pooling of `beta_raw` across a node's parents. Parents are heterogeneous quantities in different units — pooling them toward a common mean has no substantive justification, and the independent `Normal(0,1)` on the **rescaled** coefficient is deliberate. Disclosure is the MVP; pooling stays an option to argue for, not a plan* |
| S16 | **Forward-simulation variance in the trend interval** — `rca.py:1618` computes `trend_delta` from `trend_samples[:, -1]`; `t_an` is computed three lines earlier and feeds only the seasonal term, so a one-day analysis window and a ninety-day one starting the same date return the **identical** trend estimate *and* the identical CI. Add the omitted forward-simulation variance so the interval grows with the horizon | ○ | The flat *point* forecast is a deliberate, documented property of a local-level random walk (and S8/3.4 address it). The horizon-invariant *interval* is a distinct and more mechanical defect: the random walk's forward variance accumulates with each step past the fit end and is simply absent. **Measure the magnitude before quoting one** — it scales with `σ_trend`, which this model prioritizes tightly, so the size of the understatement is an open empirical question and the current external estimate is asserted rather than measured. `components.trend` is the number that gets narrated |
| S17 | **Rebuild the calibration suite's coverage test** — draw truth from the DGP rather than the realized series; add the two cases with **zero** coverage today; vary the seed per world; raise the pass bar | ○ | `_planted_step_world` computes `truth = beta * (x[an].mean() − x[ref].mean())` from the **realized** `x`, and checks it against percentiles of `beta_samples × bootstrap of that same realized x` — so the window-sampling term is pure added width around a point already equal to the truth's x-factor, and the inflation is precisely what would hide a narrow ADVI posterior. Layered on: an 80% bar for a nominal 95% interval has no power to reject true coverage of 0.85, all 20 worlds share one DGP varying only the noise seed, and `random_seed=0` is fixed across them. Two things are consequently untested anywhere — **formula-node CIs**, which come 100% from C4's bootstrap, and **collinear parents** (S4's failure). The design of 1.2 is right; this implementation cannot fail. Cheap complement to S5, not a replacement. **Note (2026-08-08):** the collinear case used to be borrowable from the reference tree; [C10](#horizon-0--correctness-numbers-the-engine-cant-defend) fixed it there, so this item now owns building the fixture |
| S18 | **Right-censored metrics — series whose past values restate.** The snapshot key is `(metric, grain, kind, window)` (`snapshots.py:_filename`) with no content hash and no TTL, so a series that rewrites its own history is frozen at whatever it said on first fetch, and a model refit from snapshots trains on values the warehouse has since changed. Options, cheapest first: content-hash or bitemporal snapshot invalidation; a declared `settlement_lag` on a node, so RCA refuses to target periods still inside it; or a settlement-lag edge type relating a booked basis to its settled one | ○ | Found at Northern Nights (2026-08-11), where payment plans settle backwards and the settled basis catches ~25% of booked early in a cycle — but the shape is general: late-arriving conversions, refunds, chargebacks, insurance claims, anything bitemporal. The engine has no way to say a number is provisional, so the honest workaround is entirely the author's discipline (fit a never-restating booked basis; keep settled as a reporting leaf, never an in-cycle RCA target). That discipline is now documented in [docs/deploying.md](../docs/deploying.md#snapshots-fetch-once-refit-forever), which makes it a *disclosed* limitation rather than a silent one — the S-item bar, not a reason to close it |
| S19 | **Partial pooling across a repeated-cycle grouping** — let nodes declare membership in a `cycle` (edition, season, cohort, campaign) and partially pool coefficients across cycles, so a node with five observations borrows strength from its own history rather than fitting each cycle alone | ○ | The thin-panel case: one product cycle a year and five editions of history ever is not a pathology, it is festivals, conferences, annual enrollment, agriculture and elections. **Deliberately not a reopening of [S15](#statistical-rigor-s--a-standing-workstream)'s rejected pooling**, and the distinction is the whole argument: S15 declined to pool `beta_raw` *across a node's parents*, because parents are heterogeneous quantities in different units with no substantive reason to share a mean. Pooling one node's *own* coefficient across repeated instances of the same cycle has exactly the justification S15 found missing — the instances are the same quantity measured again. Raised by Northern Nights (2026-08-11) |
| S20 | **Zero-inflated and count likelihoods** — the observation model is Gaussian throughout (`model.py:727`). **Sharpened 2026-08-17, and split into a cheap half and the real one:** the misspecification is currently *undisclosed* — a mostly-zero series fits, converges, and stamps `fit_quality: "ok"` (the ELBO check says only that the optimizer stopped, per [S2](#statistical-rigor-s--a-standing-workstream)) with nothing in the payload or the docs saying otherwise, which fails the S-track's own disclosed-limitation bar for the one client whose business is built of long true-zero windows. **The disclosure half shipped 2026-08-17**: when ≥25% of a node's fitted periods are exactly zero, the fit emits `likelihood_warnings` (a channel parallel to `seasonality_warnings` — not a `fit_quality` downgrade, which would conflate convergence with misspecification and put the wrong explanation under the UI's *suspect fit* text), carried on the RCA payload, rendered in the live node detail and the export's caveat block, kept by `compact_rca` with an `RCA_HOW_TO_READ` clause, and written up as limitation #7 in `docs/model.md`. Detection keys on *exact* zeros (the flow zero-fill contract writes literal 0.0; near-zero is a different regime) over the raw pre-normalization series. The likelihood itself stays scheduled here, and closing it deletes the disclosure. Offer a small, named set of alternatives (Poisson/negative-binomial for counts, a zero-inflated variant for spiky series with structural zeros), declared per node like `kind`, rather than a general likelihood language | ○ | A seasonal business has months-long true-zero windows between cycles, and a Gaussian fit to a series that is exactly zero for a third of its length puts mass on negative counts and mis-states the variance everywhere else. Distinct from [C4](#horizon-0--correctness-numbers-the-engine-cant-defend), which the same series also triggers: C4 is a degenerate *bootstrap* over a constant window, this is a misspecified *likelihood*. Fixing either alone leaves the other. Raised by Northern Nights (2026-08-11); MVP-first discipline applies — a named few, not a modeling language |
| S21 | **Fit a node with undefined periods by masking the likelihood, not refusing it** — **optional; build it when a real tree needs it, not before.** [1.11](#horizon-1--prove-it-a-trustworthy-reproducible-rca) settled the undefined-rate policies and chose *refusal* at the fit: a node with any undefined period reports `fit_failed`, naming the periods. The argument for that choice is sound and worth keeping — imputing fabricates an observation, and dropping the row re-dates every later period, which is the defect roadmap 1.1's gap-free spine check exists to prevent. But there is a third option it does not take. **Missing observations are the case a state-space model is built for**: the latent trend keeps a state at every `t` and the likelihood is simply not conditioned on the periods that have no value — mechanically `pm.Normal(..., mu=mu[mask], observed=y[mask])`, with the trend, seasonality and regressors untouched. The node keeps its posterior instead of losing it, and the uncertainty at an unobserved period widens on its own, which is the honest answer rather than a stated one. **Why it is optional rather than scheduled:** the refusal degrades gracefully today — `fit_failed` reports the node's own gap and the other 17 of 19 nodes in scope still answer — so nothing is *wrong*, only unavailable, which is the S-track bar rather than the C-track one. It costs a real posterior only on nodes that are both probabilistic *and* have undefined periods, and no shipped tree has one: White Cube's `churn_arpu` is a rate with no probabilistic parents. The trigger to build it is a client tree where a fitted node has genuine gaps — a quiet flow denominator on a learned edge — at which point the node is silently unavailable rather than merely refused. Two things to settle first if it is taken up: whether `fit_quality` must carry the observed-period count (a fit on 60 of 100 periods is not a fit on 100), and whether `MIN_FIT_PERIODS` counts observed periods or spine periods — it should be observed, or the floor is not the floor | ○ | The alternative to a posterior is no posterior, and this project's own position is that withholding is honest — so this is not a correctness gap and does not belong in Horizon 0. It is here because the *capability* is one the model already has and is not using: a Kalman-style latent trend handles a missing observation natively, and refusing the whole fit over one dark week spends a node's entire posterior to avoid a problem the model structure solves. Cheap to try (the mask is a few lines), easy to measure against the refusal on the same tree, and reversible |

*(**Considered and not scheduled: dated events as precision-aware model terms.**
Northern Nights asked for an event term carrying its own date uncertainty, their
marketing calendar tagging every row day / week / month precision. The MVP-first
answer already exists and they had independently built it: encode events as
**intensity series** at the grain their precision supports — day rows feed daily
nodes, week rows a weekly series, month rows stay out — which needs no schema at
all and keeps the engine's "no uncertainty slot for dates" honest rather than
faked. A first-class `event:` node type with a date prior is the modeling-language
scope creep [Deliberately not on the roadmap](#deliberately-not-on-the-roadmap)
rules out. What is worth taking is their **precision discipline** and their
separation of an event's *comms* (a demand spike, an intensity node) from its
*execution* (a price change, visible through the units × price identity) —
collapsing the two recreates a confound. Both are authoring guidance, not
engine work.)*

**Related, already scheduled elsewhere:** [3.4](#horizon-3--make-it-findable-and-sticky-it-comes-to-you)
(counterfactual RCA via posterior-predictive forecast) is the white paper's
fourth §4.1 item — it upgrades the flat-trend approximation and shares
machinery with S3. It stays in Horizon 3 rather than being duplicated here.

**Exit:** no fixed exit — this track is maintenance of a property, not a
milestone. The nearest thing to a bar: intervals that pass SBC (S5) *and a
coverage test that can fail* (S17), a default inference path whose approximation
error is *measured* rather than assumed (S1/S2), and no weakness in white paper
§3.2 left without either a fix or a disclosed caveat.

---

## Deliberately not on the roadmap

- Real-time / streaming grains.
- Our own **semantic layer** — governance, access control, self-serve, result
  caching, reusable dimension groups, general join planning. We ride dbt's where
  it exists. **Amended (2026-08-10):** this line previously read "our own metric
  definition language (we ride dbt's)", which was always broader than the
  intent — the tree schema has carried `priors`, `lags`, `expected_signs`,
  `kind`, `plausible` and `formula` since 0.1.0, and the `warehouse` provider's
  per-metric `sql:` was already an untyped binding. Roadmap
  [2.9](#horizon-2--make-it-repeatable-a-stranger-can-onboard) makes that last
  mile explicit as a **fetch descriptor** — how one node gets one series — and
  nothing more. The boundary test for any proposed field, and the `sql:` stop
  rule that holds it, are in
  [`semantic_layer_connectivity_design.md`](semantic_layer_connectivity_design.md)
  §4.1. What is listed above stays out permanently.
- Causal **discovery** — the DAG stays the analyst's hypothesis; that's the premise, not a gap.
- Per-seat anything.
- Dark mode (for now).
- **Hybrid cold-start → fitted mode** (was 2.7; removed 2026-08-05). Per-node
  graduation — a node with ≥ `MIN_FIT_PERIODS` whole periods uses its posterior,
  every other node falls back to prior draws, each response saying which basis it
  used. The idea is genuinely appealing (watching intervals shrink node by node
  *is* the early learning of a company, made visible) and it is removed anyway,
  for two reasons. It needs a policy for partially-fitted paths that the roadmap
  itself admitted was "worth its own small spec" — real design cost. And it
  extends cold start, now a demo mode rather than a supported persona: it would
  spend engine surface deepening the one mode whose users have no incident to
  explain. `MIN_FIT_PERIODS = 10` on a model carrying one latent trend state per
  observation is ≥ 14 latent parameters on 10 data points, so the cliff it was
  meant to soften is partly a cliff we should not be encouraging anyone over.

---

*Ticket-level detail and the rationale behind shipped work live in
[`archive/`](archive/) (statistical review T1–T12, UI plan U1–U6, connectivity
analysis). This roadmap absorbs their open items.*

---

*This document is written and maintained by an AI agent (Claude), with human oversight.*
